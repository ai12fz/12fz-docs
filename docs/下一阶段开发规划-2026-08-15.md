# 下一阶段开发规划（v1.0）

> 日期：2026-08-15 · 作者：服务器技术（qiu 侧）
> 背景：IAM 迁移重构已全部完成（001-008 生产执行、零残留、审查 5 份清零、零技术债整改收尾）。
> 本文档规划下一步工作，供讨论确认后执行。

---

## 一、当前状态基线（已完成）

| 板块 | 状态 |
|------|------|
| 身份重构（iam 8 表 / RLS 41 / 列名全清） | ✅ 生产运行中 |
| 认证（RS256 + refresh token 7 天无感续期） | ✅ 已上线 |
| 双端基线统一（marketplace/chat-token → main） | ✅ 已合并 |
| 功能审查（3 份报告 P0/P1/P2） | ✅ 清零 |
| 规范审查（响应 httpx / 日志 logx / 路由方法 / 残留清理） | ✅ 零技术债收尾 |
| 半成品模块（supply/work_order/snq/label/showcase/mrp） | 保留（待开发） |

---

## 二、待办清单（按优先级）

### 🟠 P0-1 存量业务决策（等老板拍板）

| 项 | 说明 | 建议 |
|----|------|------|
| 59 笔 -¥17.08 补扣 | 历史对账差异，涉及余额调整 | 建议一次性补扣（需确认） |
| Windows npc 隧道 | 内网穿透通道（Windows 端）未重启 | 重启后验证连通 |

### 🔵 P1 开发排期（本次讨论重点）

#### 1. RBAC 完整权限矩阵（建议先做）
- **现状**：三角色（super_admin/merchant_admin/staff）+ 17 权限，接口级权限仅部分覆盖
- **目标**：完整权限矩阵（角色 × 模块 × 操作），所有业务接口接入鉴权
- **工作量**：约 2-3 天（权限表设计 + 中间件 + 接口接入）
- **依赖**：无

#### 2. 前端切换企业 UI 增强
- **现状**：OrgSelect 已可用（登录后选企业 / switch-tenant 切换）
- **目标**：企业切换下拉（全局右上角）、当前企业标识、权限变化提示
- **工作量**：1 天
- **依赖**：RBAC（切换后角色权限刷新联动）

#### 3. 半成品模块开发（按业务优先级排队）
- **候选**：supply(供应商) / work_order(工单) / snq(质检) / label(标签) / showcase(展示) / mrp(物料需求)
- **现状**：代码骨架已存在（前后端路由/页面已挂），表未建（查询会 500）
- **启动方式**：选 1-2 个核心模块先做通（建表 → 联调 → 上线），其余排队
- **工作量**：每个模块 2-4 天（取决于业务复杂度）
- **待确认**：哪个模块业务优先级最高？

### 🟢 P2 持续维护

| 项 | 说明 |
|----|------|
| 漂移检测监控 | cron 每日 09:00 已运行，持续观察 |
| refresh token 运行观察 | 上线后观察 7 天轮换/续期是否正常 |
| 新代码规范落地 | httpx 响应 + logx 日志 + 方法路由（🅲 规矩已立） |

---

## 三、执行顺序建议

```
第一步：P0-1 两项拍板（补扣金额确认 + 隧道重启）
第二步：P1-1 RBAC 权限矩阵（地基，影响后续所有模块鉴权）
第三步：P1-2 前端切换 UI（依赖 RBAC，体验提升）
第四步：P1-3 半成品模块（选 1-2 个核心模块先做通）
持续：P2 观察项
```

---

## 四、待拍板清单

1. 59 笔补扣：做 or 不做？金额确认？
2. Windows npc 隧道：现在重启？
3. RBAC 权限矩阵：本周排期确认？
4. 半成品模块：先做哪个？（supply / work_order / snq / label / showcase / mrp）

---

*本文档为讨论稿，确认后按此执行。*
