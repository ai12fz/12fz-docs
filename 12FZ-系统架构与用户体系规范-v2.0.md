# 12FZ 系统架构与用户体系规范（总纲）v2.0

> 全系统唯一权威规范 · 2026-08-15（合并：用户体系规则 v1.5 + 身份命名规范 v3.1 + 安全规范 v1.0 + 迁移实施计划 v1.0）
> 原则（老板定案）：**彻底改进、零技术债、不考虑工作量**；代码质量与稳定性扩展性优先
> 配套：《12FZ Marketplace 开发文档 v3.35》（业务详档，开发主读）

---

## 〇、系统全景

### 0.1 域名分工（三入口一库）

| 域名 | 用途 | 后端 |
|---|---|---|
| **go.12fz.com** | 业务 API + SPA（Marketplace：订单/商城/OA/物联/财务） | Go 8099 |
| **go.12fz.com/git/** | Gitea 代码托管（OAuth2 对接 iam） | Gitea 3000 |
| **ai.12fz.com** | 聊天 WS + 主机/Agent 心跳 | chat-token 8081 |
| **ai.12fz.com/v1** | 大模型中转（OpenAI 兼容） | chat-token 8081 |

**统一数据底座**：PostgreSQL 12fzsj（用户/聊天/业务/账本同库）+ Redis 7。`uid`（数字）与 `tenant_id`（UUID）贯穿全系统。

### 0.2 两域关系（核心）

```
go.12fz.com（业务/管理门户）          ai.12fz.com（聊天/AI 服务）
  唯一登录入口，签发 JWT ───────────►  本地验签（同一身份）
  用户/商户/订单/账本                  聊天 WS + 设备心跳 + /v1 中转计费
  共用 12fzsj 库，同一 uid/tenant_id
```

- **登录链路**：go 签发 `{uid, tenant_id, roles}` JWT → ai 本地验签
- **商户贯穿**：go 开户/充值/下单，ai 用 `sh-` key 消费模型——账本/余额同一 tenant
- **设备贯穿**：`zc-` 注册码在 go 生成 → 设备注册归属 → ai 心跳/调用

### 0.3 标识前缀

| 类型 | 前缀 | 说明 |
|---|---|---|
| 中转商户 key | `sh-` | proxy_keys 唯一保留前缀 |
| Agent token | `st-` | Agent 身份 |
| 设备密钥 | `fz-` | 设备身份 |
| 设备 ID | `d_` | 设备标识 |
| 注册码 | `zc-` | 设备注册（唯一权威） |

---

## 一、用户体系规则

### 规则 1：唯一身份标识 = `uid`（BIGINT）
- 全生态唯一用户键（原 user_id 命名废止）；任何业务表禁止用 username/昵称/手机号当外键
- 值不重排：1=admin、2=测试、11-14=bot 原样继承

### 规则 2：单一签发、本地验证
- marketplace 唯一登录入口，签发 `{uid, tenant_id, roles}` JWT（短时效 2h + refresh）
- chat-token 共享密钥本地验签；无认证代理

### 规则 3：当前用户一律从 token 注入
- 所有 `/api/*` 用中间件解析的 uid；不接受 URL 传 userId/username

### 规则 4：设备/Agent 是资源身份，不是用户身份
- `d_`/`st-` token 保留（设备自己用），payload 带 `owner_uid`；好友关系挂 owner 名下

### 规则 5：登录账号（accounts 模型）
- 账号与用户分离：`iam.accounts`（login_type: password/phone/email/wechat + identifier 唯一）
- 一用户多登录方式；users 表只留 profile，不冗余 phone/email
- 微信登录：手机号/邮箱二选一为跨入口锚点（原规则 7）

### 规则 6：主机/设备归属——注册码是唯一权威
1. 新设备唯一合法途径：生成注册码（`zc-`，强制 tenant_id，空 400）→ RegisterDevice 兑换归属
2. 存量设备一律平台（`00000000-...`）；禁止按名字/IP 推断
3. 改归属：先查注册来源（host_reg_codes/创建时间/生成人），不明先问平台主；禁止直接连库改
4. 判定顺序：注册码绑定 > 创建记录 > 询问平台主

### 规则 7：归属变更执行机制
1. 合法途径：注册码流程 / 归属变更接口；禁止直接连库 UPDATE org 字段
2. 审计留痕：`iam.audit_log` 全量记录（actor_uid/ip/app/时间/新旧值）
3. 每日核对：注册码 tenant vs 主机 tenant vs key tenant——漂移即告警
4. 追责：违规直连改库以审计记录追责

### 规则 8：商户数据隔离三维度
| 维度 | 来源 | 规则 |
|---|---|---|
| ① tenant_id | 数据行字段 | NULL=平台共享；商户 UUID=私有；admin=全零 |
| ② industry_code | `org_merchant.industry_code` | 行业授权过滤 |
| ③ menu_ids | 商户类型 + allowed_menus | 菜单/功能授权 |

**四条铁律**：org 只信服务端解析 / fail-closed / 读写全覆盖 / admin 豁免（仅 super_admin）。
**三层防御**：L1 中间件（RequireTenant）→ L2 三维度过滤 → L3 RLS（41 表 FORCE + AfterConnect 默认 admin + Scoped 事务覆盖）。

---

## 二、命名与通用规范

| 术语 | 含义 | 禁止 |
|---|---|---|
| `uid` | 用户 ID（BIGINT） | ~~user_id~~、username 外键 |
| `tenant_id` | 租户/组织 ID（UUID） | ~~org_id~~ |
| `account_id` | 登录账号 ID | — |
| `owner_uid` / `creator_uid` | 操作人 | — |

**通用规范（全库强制）**：
- 审计字段 `created_at`/`updated_at`（TIMESTAMPTZ NOT NULL DEFAULT now()）；软删除 `deleted_at`
- ID：用户 BIGSERIAL（继承值）；租户/业务 UUID `gen_random_uuid()`
- 命名：小写下划线；`{域}_` 前缀（iam./platform./chat.）
- 外键显式 + 索引；JSONB 扩展；枚举 TEXT+CHECK；时区 TIMESTAMPTZ
- 表结构：`iam.users/accounts/tenants/user_tenants/roles/permissions/role_permissions/audit_log`（标准 RBAC）
- 聊天域：`contacts/user_agents/user_hosts`（uid BIGINT）+ messages `sender_type(user/agent/host) + sender_id`
- 业务域：41 个 platform_ 表统一 `tenant_id` + `owner_uid`

**API 规范**：统一 `/api/v1/`；响应 `{code, message, data}`；错误码（0/40001/40100/40300/40400/42900/50000）；分页 `{total, items}`；幂等 Idempotency-Key；access log（uid/tenant_id/耗时/状态）

---

## 三、安全规范（九维）

### 3.1 认证
bcrypt/argon2 密码；JWT 短时效 2h + refresh 轮换 + 吊销；登录防暴力（5 次验证码/10 次锁定）；微信 code 一次性 + state 防 CSRF；彻底淘汰 session- token

### 3.2 授权
RBAC 全量（中间件 fail-closed）；租户三层隔离；敏感操作二次验证（改归属/删数据/大额充值>¥1000）；admin 豁免仅 super_admin（服务端硬编码）

### 3.3 传输
HTTPS 全站 TLS1.2+ + HSTS；内部服务只监听 127.0.0.1/内网

### 3.4 数据
`proxy_keys.key_text` 等敏感字段 AES-GCM 加密存储；PII 脱敏（手机 138****1234）；备份加密留 14 天；软删除统一

### 3.5 密钥
JWT 密钥/DSN/API key 一律环境变量（0600）；季度轮换；`sh-` key 可吊销重发

### 3.6 审计监控
`iam.audit_log` 全量（身份/归属/权限变更、登录成败、敏感操作）；access log；异常告警（登录失败突增/越权/计费异常/漂移）

### 3.7 接口
限流（10000/s + 按 uid/ip 二级）；CORS 白名单；CSRF（Origin + SameSite）；参数化防注入；幂等；上传白名单

### 3.8 运维
**DB 角色拆分**：svc_billing / svc_biz / svc_readonly（消灭单一超权 app_zhongtai）；服务各用独立角色+密钥；端口收敛；go vulncheck / npm audit 定期扫描

### 3.9 安全测试（验收）
暴力破解/JWT 伪造/越权/注入/传输/敏感数据扫描/恢复演练

---

## 四、技术债清理清单（一次性消灭，零过渡层）

| # | 债 | 处置 |
|---|---|---|
| 1 | public.org_user | 迁 iam.users/accounts 后 DROP |
| 2 | org_id 列（41+ 表） | 全改 tenant_id |
| 3 | chat.*.user_id text | 全转 BIGINT uid |
| 4 | chat.friends 兼容视图 | 删除 |
| 5 | friends_bak_20260802 / friends_old | DROP |
| 6 | chat.host_codes（空表） | DROP |
| 7 | JWT payload username | 移除 |
| 8 | session- 明文 token | 彻底禁用 |
| 9 | 三处认证代理 | 删除，本地验签 |
| 10 | Gitea 独立账号 | OAuth2 对接 iam |
| 11 | RBAC 预留 | 完整落地 |
| 12 | org_change_audit 专用表 | 并入 iam.audit_log |
| 13 | 单一超权 DB 角色 | 拆 svc_billing/svc_biz/svc_readonly |

---

## 附录：迁移实施计划（一次性·执行后归档）

### 阶段 0 预检（不停机）
全库备份（含 archive）；脏值扫描（documents 13 个非数字/friends 1 个/5 张 text 表）；proxy_usage 504 万行策略（加列-分批-换列）；cron 暂停（归档/漂移/pg_dump）；Redis 清空；dry-run 克隆库演练；停机通知（Hermes/202/Windows/设备/gxo 下单支付联动）

### 阶段 1-6（停机 T+0~T+3，预估 2-4h）
1. DDL：iam schema 全套 + 41 表列改 + chat 类型转换 + messages 多态
2. 数据迁移：org_user(3 行)→users/accounts；tenants/user_tenants 初始化；owner_uid 存量 NULL；校验一致
3. 代码重构：chat-token + marketplace 全部 SQL/JWT/中间件；前端全量适配；Gitea OAuth2
4. 依赖：RLS 列名、审计并入 audit_log、漂移脚本、archive 表、Redis 重建
5. 清理：DROP 旧表/视图/代理/session-（验收后 DROP 不可逆操作）
6. 功能：API 规范、RBAC、多租户 switch-tenant、Gitea SSO、安全落地（密码哈希/key 加密/角色拆分/告警）

### 阶段 7 回归（验收清单）
登录（密码/手机/微信）/聊天（messages 多态）/中转计费（官方×2 高峰×2）/商户隔离（平台 12 商户 8）/设备注册心跳/权限 RBAC 矩阵/审计留痕/漂移无/安全测试 7 项

### 阶段 8 文档同步
本总纲版本号更新；Marketplace → v3.36；迁移记录归档

### 回滚预案
DDL 失败→备份恢复（30 分钟）；迁移校验不过→停止恢复；代码异常→回滚 commit + 恢复备份；**旧表验收通过前不 DROP**

---

## 版本记录

| 版本 | 日期 | 内容 |
|---|---|---|
| v2.0 | 08-15 | 总纲合并（规则 v1.5 + 身份规范 v3.1 + 安全规范 v1.0 + 迁移计划 v1.0） |
| v1.5 | 08-15 | 规则 8.5：归属变更执行机制 |
| v1.4 | 08-15 | 规则 8：注册码唯一权威 |
| v1.3 | 08-02 | 规则 7 定案（手机/邮箱锚点） |
| v1.0 | 08-02 | 六条规则草案 |

---

*总纲 v2.0 · 2026-08-15 · 全系统唯一权威规范*
