# 12FZ 开发规范（团队统一执行版 v2.1）

> 版本 v2.1 · 2026-08-16 · **权威定稿**（v2.0 基础上全面审查修订：8 处过时信息更新 + 4 处内部矛盾修复 + 路由策略拍板，详见文末修订记录）
> 适用范围：marketplace、chat-token、auth 等全部 12FZ 服务
> 原则：零技术债、代码质量优先、减少踩坑。本规范是**强制**规则，Code Review 按此卡。
> 依据：v4.0 总纲 + 代码审查发现 + 团队实战经验 + 技术评审意见（已逐条核实）

---

## 一、总则（团队文化）

1. **结论先行**：代码注释、PR 描述、文档都先说结论，再说细节。
2. **绝对规范，不留技术债**：废弃表/兼容层/旧 token 一次性清理，不留过渡代码。
3. **改前备份，改后验证**：涉及数据/配置/生产，改前先备份，改完先验证再重启。
4. **一套数据一个标识**：同一概念只用一个字段名、一个类型，禁止同名异义或同义异名。
5. **能自动生成的不手写**：单号、编码、主键一律代码自动生成，禁止手工维护。

---

## 二、核心铁律（违反即打回）

1. **全库禁止 `org_id` / `user_id` / `merchant_id` 列名**——统一 `tenant_id`（UUID）/ `uid`（BIGINT）
2. **禁止 `session-` 明文 token**——统一 RS256 JWT `{uid, tenant_id, roles}`；host 资源型 token 仅限 chat 设备域（见 7.6）
3. **所有 API 响应统一 `{code, message, data}`**——禁止裸 `http.Error` 混用
4. **仓库禁止提交 `.bak` / `.old` / 二进制产物 / 多版本文件**——统一 gitignore
5. **禁止 `--force push main`**；改前先 `git pull --rebase`
6. **禁止死代码**：未接进主程序的模块、引用不存在表/符号的代码，一律不提交

---

## 三、命名规范（强制）

### 3.1 身份域（最高优先级）

| 术语 | 含义 | 禁止 |
|---|---|---|
| `uid` | 用户 ID（**数字一律 BIGINT**，如 iam.users.uid、chat.hosts.id；`bot:`/`host:` 仅为字符串标识/scope 名，**不是 uid 类型**，禁止建字符串主键） | ~~user_id~~、username 当外键 |
| `tenant_id` | 租户/组织 ID（UUID，平台=`00000000-0000-0000-0000-000000000000`） | ~~org_id~~、~~merchant_id~~ |
| `account_id` | 登录账号 ID（iam.accounts） | — |
| `owner_uid` / `creator_uid` | 操作人 | ~~owner_user_id~~ |
| `login_type` | 登录方式（password/phone/email/wechat/oauth） | — |

- 身份域独立 `iam` schema：`iam.users` / `iam.accounts` / `iam.tenants` / `iam.user_tenants` / `iam.roles` / `iam.permissions`
- 业务数据统一 `tenant_id`（哪个租户）+ `owner_uid`（哪个用户）双维度归属
- 登录凭证：`iam.accounts`（identifier 唯一）；users.phone/email 仅冗余展示（触发器同步）
- **旧 org_user/org_merchant 表已在 IAM 迁移中 DROP**（数据已灌入 iam.users/tenants），**新代码禁止引用**

### 3.2 通用命名

| 规则 | 说明 |
|---|---|
| 表名 | 小写下划线、复数；**按三 schema 分层**（`iam.*` / `chat.*` / `platform.*`），表名**无冗余前缀**（`platform.orders`，不是 platform_orders） |
| 字段 | 小写下划线；布尔 `is_` 前缀（`is_active`）；时间 `_at` 后缀 |
| 枚举 | TEXT + CHECK 约束，禁魔法数字 |
| ID | 用户 BIGSERIAL；租户/业务 UUID `gen_random_uuid()`；不暴露自增序列给客户端 |
| 外键 | 显式 FK + **对应列必须建索引**；禁止无 FK 的孤儿引用 |
| Go 代码 | 变量/函数驼峰（导出大写、私有小写）；包名小写单数无下划线；常量禁魔法数字 |

---

## 四、数据库规范

### 4.1 三 Schema 分层（禁止乱放表）

| Schema | 内容 | 说明 |
|---|---|---|
| `iam.*` | 身份域（users/accounts/tenants/roles/permissions/user_tenants/audit_log） | 无前缀 |
| `chat.*` | 聊天域（contacts/user_agents/user_hosts/messages/hosts/agents/proxy_*） | 无前缀 |
| `platform.*` | 业务域（orders/products/finance_ledger...） | 无前缀（去 platform_ 前缀） |

- **禁止**在 public schema 建业务表；废弃表直接 DROP，不进 schema
- 新表建在对应 schema，建表 SQL 必须进 `migrations/`

### 4.2 审计字段（全表强制）

```sql
created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
updated_at TIMESTAMPTZ NOT NULL DEFAULT now(),
deleted_at TIMESTAMPTZ -- 软删除
```

- 查询默认 `WHERE deleted_at IS NULL`；物理删除仅运维授权
- **updated_at 维护机制（二选一，写死）**：
  - **方案 A（推荐）**：统一触发器 `trg_set_updated_at`（BEFORE UPDATE，`NEW.updated_at = now()`），新表建表时一并创建
  - **方案 B**：封进 `pkg/db` 的 Update 封装（所有 UPDATE 走统一 helper，自动带 updated_at）
  - **禁止**各 handler 手写 `updated_at`（必漏），review 必查
- 时区一律 TIMESTAMPTZ（UTC 存储，展示时区转换）
- 业务扩展用 JSONB，禁止 EAV 表

**⚠️ 软删除 × 唯一约束的坑（必踩）**：软删除 + 唯一约束会撞车——删了用户再注册，唯一索引直接报错。**唯一约束必须用部分唯一索引**：

```sql
CREATE UNIQUE INDEX uq_users_phone_active ON iam.users (phone) WHERE deleted_at IS NULL;
CREATE UNIQUE INDEX uq_users_email_active ON iam.users (email) WHERE deleted_at IS NULL;
```

新表一律照此写，禁止「软删除 + 全表唯一约束」组合。

### 4.3 迁移脚本（DDL）

- 所有表结构变更放 `migrations/` 目录，按序号命名：`NNN_描述.sql`
- DDL 必须幂等（`IF NOT EXISTS` / `ADD COLUMN IF NOT EXISTS` / `DROP CONSTRAINT IF EXISTS`）
- **迁移执行记录表**：建 `schema_migrations`（version PK / description / applied_at / env）记录每个环境（183 生产 / 202 dev）执行状态，防止漏跑/重跑：

```sql
CREATE TABLE IF NOT EXISTS public.schema_migrations (
  version      BIGINT PRIMARY KEY,
  description  TEXT NOT NULL,
  applied_at   TIMESTAMPTZ NOT NULL DEFAULT now(),
  env          TEXT NOT NULL DEFAULT current_setting('app.env', true)
);
```

- 每个 migration 同步执行到所有环境，并在 schema_migrations 登记
- **禁止「已写代码未建表」的半成品模块提交**——要么建表脚本一起提交，要么不提交代码

### 4.4 执行规范（重要，全是实测坑）

- **表 owner 不一致**：部分表 owner 是 `postgres`，部分 `app_zhongtai`。改表结构前先 `pg_tables` 查 owner，用正确用户执行
- **新表统一 owner**：新建表一律 `ALTER TABLE ... OWNER TO app_zhongtai`，禁止再出现 postgres 属主业务表（消除分裂）
- **RLS 隔离**：很多表有行级安全策略，psql 直连看不到数据属正常现象；排查数据用接口或用 `postgres` 超级用户
- **外键改列名/类型**：先 `DROP CONSTRAINT` 再改，否则报「incompatible types」
- **改列类型前先扫脏值**（如 text→bigint 先确认全是纯数字）
- **大表按月分区**：`chat.proxy_usage` / `platform.finance_ledger` 禁止无限单表增长
- **变更红线**：生产库改结构/删数据 = 危险操作：先备份（pg_dump），改完验证，留回滚方案
- **禁止在 SSH heredoc 里写复杂 SQL**（引号/`$` 转义必出错），本地写好文件再执行

### 4.5 RLS（行级安全）

- 业务表启用 RLS：`current_setting('app.tenant_id')` 策略 + FORCE
- 代码侧必须 `SELECT set_config('app.tenant_id', $1, true)`（事务级）
- **super_admin 豁免规则（明确写死，默认隔离）**：
  - super_admin **默认与其他账号一样隔离**（列表按 tenant_id 过滤，防普通管理员误用 super_admin 账号看到全量商户数据）
  - **只有显式传 `all_tenants=true` 才查全量**（全局报表/跨租户运维等明确场景）
  - 普通账号缺 tenant_id = **401/403（fail-closed）**，禁止兜底到平台租户
  - 实现：`tenant_id` 过滤条件 = `(CASE WHEN $all_tenants THEN TRUE ELSE tenant_id=$tenant END)`；`all_tenants` 仅 super_admin 可传，否则 40300
  - 这是最容易写错的地方，review 必查

---

## 五、Go 代码规范

### 5.1 目录结构

```plaintext
cmd/{服务名}/        # 入口 main.go
internal/
  {模块}/handler.go  # HTTP handler
  {模块}/...         # 业务逻辑
  router/router.go   # 路由注册
  middleware/        # 中间件（RBAC/JWT/审计）
  db/                # 连接池
pkg/                 # 可复用包（如 pkg/sn 序列号）
migrations/          # 数据库迁移脚本（DDL）
```

### 5.2 格式门禁（强制，提交前必须过）

```bash
gofmt -w ./internal ./cmd && goimports -w ./internal ./cmd
```

- gofmt 未格式化禁止提交；import 分组（标准库 / 第三方 / 内部包）
- 一行不超过 120 字符（软性）

### 5.3 质量门禁（强制，全绿才可合并）

```bash
go build ./...   # 全量编译，不是只 build main！
go vet ./...     # 静态检查清零
```

- ❌ 禁止「只 `go build ./cmd/xxx` 通过就以为没问题」
- ❌ 禁止死代码包（写了没接进主程序、引用未定义符号、import 未使用）

### 5.4 Handler 模式（统一）

> 注：以下 `writeOK` / `writeErr` 即 `httpx` 封装的简写（成功 `{code:0, data}`；失败 `{code:业务码, message}`，业务码见 7.3 错误码表，如 40100/40300）
> **2026-08-16 修订：本模式以生产代码实测为准**（warehouse 模块是样板）。权限判断**用 `RequirePerm` 中间件**（角色/权限码在中间件层注入），handler 内 `orgScope` 只取 tenant_id + isAdmin，不再手动 `slices.Contains(roles)`（v2.0 示例的 roles 判断方式已废弃，见 5.8）。

```go
// 路由注册（router.go）——权限在路由层卡，handler 不重复判断
mux.HandleFunc("POST /xxx", mw.RequirePerm("xxx:write", h.Create))
mux.HandleFunc("GET /xxx", mw.RequirePerm("xxx:read", h.List))
```

```go
// orgScope 实测版：从 context 取 tenant_id（fail-closed），isAdmin=平台管理员
func (h *Handler) orgScope(r *http.Request) (string, bool, error) {
    orgID, err := middleware.RequireOrg(r)
    if err != nil {
        return "", false, err
    }
    return orgID, middleware.IsAdmin(orgID), nil
}

func (h *Handler) XxxList(w http.ResponseWriter, r *http.Request) {
    orgID, isAdmin, err := h.orgScope(r) // 1. 租户隔离（fail-closed）
    if err != nil { writeErr(w, 40100, "missing tenant_id"); return }

    // 2. 列表一律按 tenant_id 隔离；super_admin 显式 all_tenants=true 才查全量（见 4.5）
    q := `SELECT ... FROM platform.xxx
          WHERE ($1::bool OR tenant_id=$2::uuid) ORDER BY created_at DESC`
    rows, err := h.pool.Query(r.Context(), q, isAdmin, orgID)
    ...
    writeOK(w, map[string]interface{}{"records": out, "total": total, "size": pageSize, "current": page})
}

func (h *Handler) XxxCreate(w http.ResponseWriter, r *http.Request) {
    orgID, _, err := h.orgScope(r)
    if err != nil { writeErr(w, 40100, "missing tenant_id"); return }
    // 权限已在 RequirePerm 中间件层卡过，handler 不再重复判断
    // 3. 单号/编码自动生成
    no := genBizNo("WO") // 前缀按模块定：WO=工单、CO=定制单、RK=入库…（见 5.6）
    _, err = h.pool.Exec(r.Context(), `INSERT INTO platform.xxx (...) VALUES (...)`, ...)
    writeOK(w, map[string]string{"status": "created", "xxx_no": no})
}
```

- **响应统一**：成功 `writeOK`（`{code:0, data}`），失败 `writeErr`（`{code:业务码, message}`），**禁止用裸 HTTP 状态码当业务码**（401→40100）
- **租户隔离**：tenant_id 从 context 取（服务端解析），不信任前端传的 tenant_id
- **权限判断**：路由层 `RequirePerm(permCode, handler)` 中间件，权限码 `xxx:read/write`；**禁止 is_owner/isAdmin 布尔魔法判断**（见 5.8）；super_admin 豁免只体现在 all_tenants 查询参数（见 4.5）
- **写操作归属**：`operator := middleware.UserIDFrom(r.Context())`
- **分页**：入参 `page`/`page_size`（非 limit/offset），出参 `{records, total, size, current}`（见 7.4）

### 5.5 事务与一致性

- 涉及多表写（入库确认、调拨流转）必须用事务 `tx`
- 状态流转要在同一次操作里完成（如「出库」同时更新 SN 状态 + 调拨单状态，漏一个就是 bug）

### 5.6 单号/编码生成

- 统一 `genBizNo(prefix)`：`前缀 + 时间戳 + 5位随机`（如 `RK20260815150405xxxxx`）
- **落地位置（强制）**：封装在 **`pkg/bizno` 全局包**，各模块调用，**禁止各自实现**（否则会出现 order 一套、smartfactory 一套的两套单号）；新增前缀先查 `pkg/bizno` 前缀表避免冲突（WO=工单、CO=定制单、RK=入库、CK=外采…）
- **唯一性保障（防竞态，写死）**：
  - 单号列必须建 **UNIQUE 索引**（`CREATE UNIQUE INDEX ... ON platform.xxx (xxx_no)`）
  - 插入用 `ON CONFLICT (xxx_no) DO NOTHING` 或捕获 23505 唯一冲突，**冲突则重试一次**（重新生成号）
  - 编码查重（先查后插）降级为辅助手段——**多实例部署时查重存在竞态，不能作为唯一保障**
- 编码查重要覆盖所有同名编码表（如 KW 编码同时查 warehouse.code + loc_struct.location_code），保证全局唯一

### 5.7 日志

- 统一 logger（带 trace_id/uid/耗时），禁止 `fmt.Println` / 裸 `log.Printf` 直打业务日志
- 禁止打印 token/密码/openid 等敏感值

### 5.8 RBAC

- 权限判断用 `role.code`（super_admin/merchant_admin/staff），**禁止 is_owner 布尔魔法判断**
- 商户隔离：`RequireTenant` fail-closed，禁止从 query/body 取 tenant_id

### 5.9 SQL

- 表名必须带 schema 前缀（`platform.orders`、`chat.messages`、`iam.users`）
- **旧列名 → 目标列名映射表（强制按此迁移）**：

| 旧列名（禁止） | 目标列名（必须） |
|---|---|
| org_id | tenant_id |
| merchant_id | tenant_id |
| user_id | uid |
| sender_id | sender_uid |
| from_id / to_id | from_uid / to_uid |
| contact_id | contact_uid |
| created_by | creator_uid |
| updated_by | updater_uid |
| owner_user_id | owner_uid |

- 多行 SQL 用反引号 raw string，参数一律 `$N` 占位（防注入）
- 涉及列改名，必须同步改所有代码引用（提交前 `grep` 残留 = 0）

---

## 六、前端 Vue/TS 规范

### 6.1 格式门禁（强制）

```bash
npm run lint -- --fix
```

- ESLint + Prettier 统一；禁止 `console.log`（调试用完即删）；日志走统一 logger
- 类型用 TypeScript，禁止裸 `any`（除非确有必要并注释说明）

### 6.2 组件库（强制 @12fz/ui）

**现状确认：`@12fz/ui` 是 pnpm workspace 内部包（`"@12fz/ui": "workspace:*"`），已在 720+ 处使用，可用。** 新页面必须使用统一组件，CI 会检查：
- `PageLayout` 页面容器
- `DataTable` 数据表格
- `SearchForm` 搜索表单
- `GlobalDialog` 弹窗（`@save` 触发保存，`v-model` 控制显隐）
- `FormPage` / `DetailCard` 表单/详情

禁止裸写 `<el-table>` 等（除非大屏/可视化页面豁免）。

### 6.3 取数规范（防取数错位，血泪教训）

后端列表返回 `{records, total, size, current}`（见 7.4），前端统一：

```plaintext
const res: any = await get('/xxx')
const d = res?.data
list.value = Array.isArray(d) ? d : (d?.records || [])
```

禁止 `list.value = res?.data || []`（拿到对象不是数组，表格空）。

### 6.4 状态/类型列展示

- 纯展示的枚举/状态，用灰色文字或轻量样式，不用 el-tag 按钮样式（避免用户误以为可点）
- 有操作含义的布尔（如启用/禁用）才考虑 el-switch

### 6.5 交互健壮性

- 拖拽/pointer 交互必须加超时兜底：事件丢失时自动清理状态，防止「点了没反应」卡死
- 全局状态（pinia）的清理不能只依赖单一事件（pointerup），要加 blur/cancel/超时多路兜底

### 6.6 目录与 API

- 页面统一 `web/src/pages/` 下，**小写 kebab-case**（`order-list.vue`）；禁止大小写混用、拼写错误目录
- 组件统一进 `web/src/components/` 或共享库 `@12fz/ui`
- 列表页 onMounted 自动加载；查询条件变更重置页码到 1
- 统一走 `@/api/request.ts`（jwtToken 拦截器 + 401 跳登录）；业务 API 按域分文件，**.ts 统一**
- 字段名与后端一致（tenant_id/uid），禁止 org_id/user_id

---

## 七、API 规范

### 7.1 路由（当前状态：待迁移，有 deadline）

- **目标**：统一 `/api/v1/` 前缀
- **现状**：存量接口仍为 `/sys/`、`/api/`（nginx 有 `/api/ → 8099`、`/sys/* → 8099` 兜底在跑；chat-token 用 `/api/friend-messages`、`/api/hosts`）
- **迁移窗口（统一以 10.4 排期为准）**：
  - **新模块路由策略（2026-08-16 老板拍板，方案 B）**：新模块（如智能工厂 smart-factory）**暂不强制 /api/v1/**，沿用裸路径（如 `/custom-orders`，nginx `/api/` 重写已兼容），与存量一致；`/api/v1/` 迁移随 P3 一次性统一，避免新模块刚开工就背两套路由
  - 存量 `/sys/`、`/api/` 随 10.4 排期的 P2/P3 阶段逐批迁移（P3 约 2026-10 完成）
  - **迁移前必须盘点外部调用方**：10.10.11.150（GUNGZHOU bridge，计划任务 Hermes12FZBridge 依赖 `/api/friend-messages`）、nginx（`/api/→8099`、`/sys/*→8099`）、Feishu 通道等，**同步改完才允许下线旧路径**，否则消息链直接断
  - nginx 旧路径 `/api/`、`/sys/` 迁移完成后 301 过渡至 `/api/v1/`，过渡期 1 个月后下线
  - 迁移完成前禁止新增旧前缀路由（裸路径新模块除外，见上）
- 必须用方法路由：`mux.HandleFunc("GET /api/v1/xxx", ...)`（禁止裸路径无方法限定）
- RESTful：`GET /资源`（列表）、`POST /资源`（创建）、`POST /资源/{id}/动作`（状态流转）

### 7.2 认证
- `Authorization: Bearer <RS256 JWT>`（payload: `{uid, tenant_id, roles, exp}`；尖括号为占位符，实际 token 不带尖括号）
- 中间件解析注入 context；**禁止从 URL/query/body 取 uid**
- 当前用户一律从 token 注入；不信任任何 X-* 头
- **存量兼容**：迁移期内旧接口可临时接受 session- 或 host token，但**新接口一律 JWT**；session- 全线 401 的时点 = **2026-08-18（72h 双 token 兼容期结束，已定稿，见 10.4）**

### 7.3 响应结构（强制统一）

```json
{ "code": 0, "message": "ok", "data": { } }
```

- **必须用 `httpx.WriteJSON`**；禁止裸 `http.Error`、禁止旧 writeJSON 混用
- 错误码表（业务码 + HTTP 语义）：

| code | HTTP | 含义 |
|---|---|---|
| 0 | 200 | 成功 |
| 40001 | 400 | 参数错误 |
| 40100 | 401 | 未认证 |
| 40300 | 403 | 无权限 |
| 40400 | 404 | 不存在 |
| 40900 | 409 | 冲突（状态不允许该操作）。注：后端 `httpx` 需补 `http.StatusConflict → 40900` 映射，状态流转接口依赖此码 |
| 42900 | 429 | 限流 |
| 50000 | 500 | 服务端错误 |

- 业务错误用明确 message（如「该 SN 已在库」「状态不允许出库」）
- **业务错误码分配规则（写死，二选一）**：
  - **方案 A（推荐）**：模块分段——`41xxx`=订单、`42xxx`=库存/仓库、`43xxx`=物流、`44xxx`=财务/计费、`45xxx`=用户/权限、`46xxx`=聊天、`47xxx`=AI/模型，段内从 `xx00` 起递增
  - **方案 B**：通用码 40001/40300 等 + message 表达具体业务错误（不新增业务码）
  - 新接口二选一，**禁止混用**；新增业务码先查表避免重复

### 7.4 分页 / 幂等
- 分页：`?page=&page_size=` → 响应 **`{records, total, size, current}`**（全规范统一，前端取 `data.records`）
- 写操作支持 `Idempotency-Key` header
- 所有接口 access log（uid/tenant_id/耗时/状态，不含敏感值）

### 7.5 状态流转接口

状态机操作（出库/入库/打包/发货/确认）遵循：
1. 校验当前状态是否允许该操作（不允许返回 40900）
2. 执行操作 + 同一次更新状态
3. 返回新状态

禁止「改了业务数据但忘了改状态」的残缺流转。

### 7.6 host 资源型 token（特殊通道，单开一节定义）

**背景**：chat.hosts 使用 `d_` 前缀 token（生产 7 条全是 d_），friend-messages 可用任意 host token——这是**设备间通信的合法特殊通道**，不是违规，但必须限定范围：

- **合法场景**：仅 chat.* 域设备间通信（host/agent 心跳、消息投递）
- **权限边界**：只能调白名单接口（hosts 注册/心跳、friend-messages 收发、agents 状态），**禁止访问 platform.* / iam.* 业务接口**
- **实现**：独立中间件 `HostTokenAuth`，校验 `d_` token → 解析 host uid → 注入 context（角色恒为 host），与用户 JWT 中间件互斥
- **禁止**：host token 访问用户数据、跨租户操作、管理接口
- **迁移**：后续若统一，host token 可换成「RS256 + scope 白名单」签名（资源标识 `host:xxx` 为字符串 scope 名，**不是 uid 类型**，数字 uid 仍为 BIGINT 存 chat.hosts.id），但当前 d_ 通道合法保留

---

## 八、Git 工作流规范

### 8.1 分支
```
main # 生产，只接受 merge
feat/xxx # 功能
fix/xxx # 修复
refactor/xxx # 重构
```

### 8.2 Commit Message（Conventional Commits，强制）
```
<type>(<scope>): <subject>
# type: feat/fix/refactor/cleanup/chore/style/docs/perf
# 例：fix(调拨): 出库后更新调拨单状态pending→shipped
```
- 一次提交只做一件事，不混功能+重构+清理
- 提交前 `git diff` 自查，确认无调试残留、无无关改动

### 8.3 红线
- 永远不 `--force push main`
- 改文件前先 `git fetch + pull` 确认最新，再动手；改完 `git diff` 确认只改了自己的部分
- push 前 `git pull --rebase origin main`，避免覆盖他人提交
- 涉及数据库变更的 commit，同时带 migration 文件 + 说明执行了哪些环境（并登记 schema_migrations）

### 8.4 禁止提交
- `.bak` / `.bakYYYYMMDD` / `.orig` / `.old` / `.tmp` / `.test` / `debug*`
- 编译产物、`node_modules`、`dist`
- 工具类文件加 `//go:build tools` 或放 `tools/` 目录，并保证可编译

---

## 九、安全规范

### 9.0 环境变量总表（必配项，一页看完）

> 新服务/新同事第一件事：按此表配齐 env，**禁止代码内默认值兑底**（见 9 安全规范密钥条）

| 服务 | 变量 | 用途 | 必配 |
|---|---|---|---|
| marketplace | DATABASE_URL | PG 连接（search_path=public,platform） | ✅ |
| marketplace | JWT_PRIVATE_KEY_FILE / JWT_PUBLIC_KEY_FILE | RS256 签发/验签 | ✅ |
| marketplace | JWT_KID | JWKS kid（与公钥指纹一致） | ✅ |
| marketplace | PORT | 监听端口（8099） | ✅ |
| chat-token | PG_CONN | PG 连接 | ✅ |
| chat-token | JWT_PUBLIC_KEY_FILE / JWT_KID | RS256 验签 | ✅ |
| chat-token | **JWT_SECRET** | **2026-08-16 实测：已配（systemd Environment=JWT_SECRET 随机值），config.go 默认值已封死为 ""，兜底链删除** | ✅ 已配 |
| chat-token | SSO_SECRET / BOT_TOKENS | 存量明文，限期迁 env 文件（当前在 /etc/12fz/chat-token.env，**待迁纯 env 管理**） | ⚠️ 进行中 |
| 双侧 | WECHAT_APPID / WECHAT_APPSECRET | 微信通道 | ✅ |
| 双侧 | API_KEY 相关 | 上游服务密钥，应用层加密存储 | ✅ |
| 注 | **变量名统一**：两服务连接串变量名不一致（现状 DATABASE_URL vs PG_CONN），后续统一为 `DATABASE_URL`，存量兼容期可双支持（读 PG_CONN 优先，回退 DATABASE_URL） | — |

- env 文件统一放 `/etc/12fz/*.env`，`chmod 600`，systemd `EnvironmentFile=` 引用
- 新增服务必须补本表一行后再合代码

### 9.1 认证与密钥

| 项 | 规则 |
|---|---|
| 认证 | RS256 JWT + JWKS/kid 轮换；access 7 天 + refresh 7 天（**2026-08-16 老板拍板：access 由 15min 放宽为 7 天**，配套 401 自动刷新已就绪）；session- 迁移期内过渡，401 时点 = 2026-08-18（72h 兼容期结束，已定稿） |
| 密码 | bcrypt（成本 12）/argon2，禁明文/弱哈希/MD5；登录防暴力（5 次验证码/10 次锁定） |
| 密钥 | 走环境变量（os.Getenv），禁硬编码；**systemd 配置中的明文密钥（SSO_SECRET/BOT_TOKENS/APP_SECRET）限期迁移到 env 文件（/etc/12fz/*.env）并封死代码默认值**；**点名 `JWT_SECRET`（chat-token）：当前未设环境变量，config.go 有默认值兜底链 `JWT_SECRET → SSO_SECRET → "12fz-sso-2026"`——密钥可预测，等于所有 token 可伪造，必须同批迁入 env 并删默认值**；api_key 应用层加密存储，不落明文；不进日志 |
| 审计 | iam.audit_log append-only（业务账号无 UPDATE/DELETE）；PII 脱敏 |
| 数据隔离 | RLS 按 tenant_id + 三维度（tenant_id/industry_code/menu_ids）；super_admin **默认隔离**，显式 `all_tenants=true` 才查全量，普通账号缺=401/403 fail-closed（见 4.5） |
| 越权 | 跨租户访问必须 40300；switch-tenant 校验归属 |
| JWT 配对 | 私钥只在签发方、公钥验签方，双侧必须配对（提交前验证指纹一致） |

---

## 十、CI/CD 部署规范

### 10.0 规范落地待办（文档已定义、代码尚未实现的清单）

> 以下条目规范已要求，但生产代码尚未落地，**优先补齐后再执行对应功能**：

| # | 项 | 现状 | 落地动作 |
|---|---|---|---|
| 1 | 40900 错误码 | ✅ **已实现**（httpx/write.go 已有 StatusConflict→40900，2026-08-16 实测） | 完成，标记 ✓ |
| 2 | HostTokenAuth 中间件 | **未实现**（chat-token middleware 仅 cors.go） | 新建 HostTokenAuth：校验 d_ token → host uid → 白名单路由（见 7.6） |
| 3 | 分页参数 page_size | 现状 `limit`(45处)/`offset`，响应 `items`(42)/`records`(25) 混用 | 统一为 `page/page_size` + `{records,total,size,current}`，随 10.4 排期分批改 |
| 4 | 响应 {code,message,data} | 存量 writeJSON/http.Error 未清（实测 writeJSON 残留 213 处） | 随 10.4 排期分批迁 httpx |
| 5 | 密钥 env 化 | JWT_SECRET 已配 ✓；SSO_SECRET/BOT_TOKENS 仍在 env 文件明文，待迁纯 env 管理 | 迁 /etc/12fz/*.env，代码默认值封死 |
| 6 | CI 门禁 | ✅ **已落地**（.gitea/workflows/check.yml + ci.yml 已存在，2026-08-16 实测） | 完成，标记 ✓ |
| 7 | **genBizNo 统一单号（新增）** | **未实现**（order 仍用 S+时间戳+随机，全库无 genBizNo） | 补 pkg 级 genBizNo + UNIQUE + ON CONFLICT 重试（见 5.6），智能工厂模块先行落地 |

### 10.1 部署流程
1. 本地编译验证：`go build ./cmd/marketplace/`
2. commit + push 触发 Gitea CI（check + build-and-deploy）
3. 查 CI run 状态（`actions/runs`）确认 success
4. 前端验证：curl 生产 chunk 确认新代码生效

### 10.1.1 CI 门禁（强制，不过直接 fail build）

Gitea CI 的 check 阶段必须加以下检查，任一失败即 fail build，**禁止依赖人工自觉**：

```yaml
# .gitea/workflows/check.yml（示例）
- name: gofmt check
  run: |
    unformatted=$(gofmt -l ./internal ./cmd)
    if [ -n "$unformatted" ]; then
      echo "未格式化文件:"; echo "$unformatted"; exit 1
    fi
- name: go vet
  run: go vet ./...
- name: go build
  run: go build ./...
```

- 后端：`gofmt -l` 输出为空 + `go vet ./...` 清零 + `go build ./...` 通过
- 前端：`npm run lint` 通过（ESLint 硬性，禁 console.log）
- CI 结果以 `head_sha` 为准（Gitea run 编号可能错乱）

### 10.2 已知坑（必看）
- **CI 的 vite 包会损坏**：`node_modules/.pnpm/vite@*/dist/client/client.mjs` 可能被清空，导致前端构建失败报 `cannot test case insensitive FS`。修法：删损坏的 vite 包目录 + `pnpm install --frozen-lockfile` 重装
- **Gitea API 数据错乱**：run 编号和 commit 映射可能不一致、状态显示 queued 实际已 success。以 `head_sha` 为准，别只看 run_number
- **scp 覆盖运行中二进制报 Text file busy**：必须 `systemctl stop → mv → systemctl start`，不能直接 scp 覆盖
- **部署前端目录是 /opt/marketplace/web-dist**（不是 /var/www/marketplace，那是旧目录）
- **部署目录禁止堆积 .bak**——旧二进制统一移 `/backup/` 或删除

### 10.4 迁移排期（deadline 重估，四件大事不挤一个窗口）

> 四件迁移叠加工作量 = 单做 IAM 的 3-4 倍，必须按序分阶段，禁止挤在同一窗口。
> **2026-08-16 修订（实测收尾状态）**：P0（列名统一）、P1（三 schema 分层）**生产库已收尾**——platform 90 表 / iam 8 表 / chat 19 表已就位，全库 org_id/user_id/merchant_id 残留=0（schema_migrations 001-010 已执行）；仅剩 1 张遗留表 `order_items_legacy_20260815` 待清理。

| 优先级 | 迁移 | 状态 | 说明 | 建议窗口 |
|---|---|---|---|---|
| **P0** | uid/tenant_id 列名统一 | ✅ **已收尾** | 全库 org_id/user_id/merchant_id 残留=0；剩 1 张 legacy 表待 DROP | 收尾 2026-08 底前 |
| **P1** | 三 schema 分层 + 去前缀 | ✅ **已收尾** | platform 90 / iam 8 / chat 19 表已就位；旧表 DROP 完成 | 收尾 2026-09 |
| **P2** | 响应结构 {code,message,data} + 分页 {records,total,size,current} | 待做 | 所有 handler + 前端同步（writeJSON 残留 213 处） | 2026-09~10 |
| **P3** | API 前缀 /api/v1/ | 待做 | nginx 301 过渡；**新模块（智能工厂等）暂沿用裸路径，随本阶段一并迁** | 2026-10 |

- 每阶段独立评审 + 回归，**不积压到月底集中爆**
- 各阶段同步更新 10.0 落地待办表状态
- **session- 双 token 兼容期 72h（已定稿）**：2026-08-15 22:15 迁移完成 → **2026-08-18 后全线 401**，7.2 与此一致（替代 v2.0 的"以 10.4 排期为准"表述）

### 10.5 手动部署（CI 挂掉时的备选）

```bash
# 后端
go build -o /tmp/marketplace ./cmd/marketplace/
scp /tmp/marketplace root@8.138.235.183:/opt/marketplace/marketplace.new
ssh root@... 'systemctl stop marketplace && mv .../marketplace.new .../marketplace && systemctl start marketplace'

# 前端（scp 单文件 + 构建）
scp web/src/xxx.vue root@...:/tmp/mp/web/src/xxx.vue
ssh root@... 'cd /tmp/mp/web && pnpm --filter marketplace-frontend build && cp -r dist /opt/marketplace/web-dist'
```

---

## 十一、禁止事项（红线，Code Review 直接打回）

1. ❌ **死代码**：未接进主程序的模块、引用不存在表/符号的代码
2. ❌ **硬编码密钥/密码/内网 IP**：一律走环境变量或配置中心
3. ❌ **调试残留**：`console.log`、`fmt.Println` 调试输出、临时 panic
4. ❌ **TODO/FIXME 堆积**：能做的当场做，不能做的提 issue，不留 TODO 在代码里
5. ❌ **复制粘贴代码**：重复逻辑抽公共函数
6. ❌ **忽略错误**：`_ = err`、空 `catch`，必须显式处理或注释说明
7. ❌ **跨服务密钥不配对**：JWT/密钥改动必须双侧同步验证
8. ❌ **旧列名/旧 token 残留**：org_id/user_id/merchant_id、session-、裸 http.Error、无方法限定路由、is_owner 魔法判断
9. ❌ **只编译 main 不当全量编译**：必须 `go build ./...` + `go vet ./...` 全绿
10. ❌ **编辑接口当创建用（INSERT 新行）**：带 id 走 UPDATE，不带 id 走 INSERT
11. ❌ **host token 越权**：d_ token 访问 platform.* / iam.* 接口（仅限 chat.* 白名单，详见 7.6）

---

## 十二、提交前自检清单（Checklist，逐条打勾）

```
□ gofmt -w 已跑，无格式 diff
□ go build ./... 通过（全量，非单 main）
□ go vet ./... 清零
□ 无死代码、无未建表的半成品模块
□ 无 console.log / fmt.Println / TODO 调试残留
□ 无硬编码密钥/密码/IP
□ 无 .bak/.tmp/多版本文件
□ 数据库改动带迁移脚本 + 已在测试库演练 + 登记 schema_migrations
□ JWT/密钥改动已双侧验证（指纹一致）
□ commit message 符合 Conventional Commits
□ 涉及列改名，已同步改所有代码引用（grep 残留 = 0）
□ 响应走 httpx + 标准错误码
□ 表名带 schema 前缀
□ 前端字段与后端一致、目录命名规范
□ 新表有 created_at/updated_at + 外键索引 + 软删除用部分唯一索引
□ RLS 策略 + app.tenant_id 设置 + super_admin 豁免规则正确
□ 分页响应统一 {records, total, size, current}（前端取 data.records）
□ 改列名/类型前先扫脏值（如 text→bigint 先确认全数字）
□ updated_at 走触发器或 pkg/db 封装（不手写）
□ 单号列有 UNIQUE 索引 + ON CONFLICT 重试（不靠查重防撞号）
□ env 已按 9.0 总表配齐，无代码默认值兑底
□ 状态流转同一次操作更新状态
□ host token 仅限 chat.* 白名单接口
```

---

## 十三、常见坑（血泪教训，务必避免）

| # | 坑 | 教训 |
|---|---|---|
| 1 | 列表返回 `{records}` 前端当数组用 | 取数统一 `Array.isArray(d) ? d : (d?.records \|\| [])` |
| 2 | 外键改了引用表但漏改约束 | 改引用必须同步外键，否则 23503 |
| 3 | 状态流转只改数据不改状态 | 出库/入库/确认必须同一次操作改状态 |
| 4 | 编码查重只查一张表 | 同名编码（KW）要查所有相关表保证全局唯一 |
| 5 | 拖拽/pointer 状态卡死 | 事件清理加多路兜底（blur/cancel/超时） |
| 6 | 平台 admin 列表不隔离 | 所有列表无条件按 tenant_id 隔离（super_admin 显式 all_tenants=true 才查全量，见 4.5） |
| 7 | 编辑接口当创建用（INSERT 新行） | 带 id 走 UPDATE，不带 id 走 INSERT |
| 8 | 状态/类型列用 el-tag 像按钮 | 纯展示用灰色文字，别用按钮样式 |
| 9 | 表 owner 不一致导致 ALTER 失败 | 改表结构前先查 pg_tables owner |
| 10 | RLS 下 psql 直连看不到数据 | 属正常现象，用接口或超级用户排查 |
| 11 | scp 覆盖运行中二进制 Text file busy | systemctl stop → mv → start |
| 12 | SSH heredoc 写复杂 SQL 转义出错 | 本地写好文件再执行 |
| 13 | 软删除 + 唯一约束撞车 | 唯一约束必须 WHERE deleted_at IS NULL 部分索引 |
| 14 | systemd 里明文密钥 | 迁移到 /etc/12fz/*.env，代码默认值封死 |

---

## 十四、Code Review 规则

- **未过自检清单的 MR 直接打回，不进入 review**
- 审查重点：死代码、编译全绿、命名规范、安全红线（第九、十一节）
- 功能正确 ≠ 通过；代码卫生不达标同样打回
- 大改动（如本次 IAM 迁移）必须附：迁移脚本 + 演练结果 + 双侧验证
- **新模块样板**：智能工厂（smart-factory，原"生产系统/服装厂"，2026-08-16 老板定名）作为首个完全合规样板——`platform.styles` / `platform.work_orders` 等新表按三 schema 建、身份域全走 iam，供其他模块抄（样板 DDL 见 12fz-iam-migration/生产系统样板-DDL草案-2026-08-15.md）

### 审查红线速查

| 红线 | 处理 |
|---|---|
| 出现 org_id/user_id/merchant_id | 打回 |
| 裸 http.Error / 旧 writeJSON | 打回 |
| .bak 提交 / force push main | 打回 + 记录 |
| 无方法限定裸路径路由 | 打回 |
| is_owner 魔法判断 | 打回 |
| 表名无 schema 前缀 | 打回 |
| go build ./... / go vet 非全绿 | 打回 |
| 死代码 / 未建表半成品 | 打回 |
| 列表取数不做 Array.isArray 判断 | 打回 |
| host token 访问业务接口 | 打回 |
| 软删除表全表唯一约束（无部分索引） | 打回 |

---

*12FZ 开发规范 v2.1（权威定稿）· 2026-08-16 · 全员统一执行*
*本文档持续维护，遇到新坑随时补充。违反规范 = 审查打回；技术债 = 不合并。*

---

## 附录：v2.0 → v2.1 修订记录（2026-08-16 全面审查）

> 审查方法：规范逐条 × 生产代码/数据库/服务器实测（marketplace + chat-token + 生产库 12fzsj）

| # | 修订项 | v2.0 原文 | v2.1 定稿 | 依据 |
|---|---|---|---|---|
| 1 | 7.1 路由前缀 | 新接口一律 /api/v1/ | **新模块沿用裸路径，/api/v1/ 随 P3 统一迁（老板拍板方案 B）** | 全库实测 0 个 /api/v1 路由，nginx 仅 /api/ 重写，硬执行会断路由 |
| 2 | 7.2/9.1/10.4 session- 时点 | 以 10.4 排期为准（2026-10） | **2026-08-18（72h 双 token 兼容期结束，已定稿）** | 综合定稿方案 D3 |
| 3 | 5.4 Handler 模式 | orgScope 返回 roles + slices.Contains | **RequirePerm 中间件 + orgScope 返回 (tenantID, isAdmin) 实测版** | warehouse 模块为样板 |
| 4 | 9.0 JWT_SECRET | 未设、有兑底链、紧急 | **已配（systemd 随机值），config.go 默认值封死为 ""** | 生产实测 |
| 5 | 10.0 待办 #1 40900 | 未做 | **✅ 已实现**（httpx/write.go:21） | 生产实测 |
| 6 | 10.0 待办 #6 CI 门禁 | 未加 | **✅ 已落地**（check.yml + ci.yml 存在） | 代码实测 |
| 7 | 10.0 新增 #7 genBizNo | 未列 | **新增待办：全库无 genBizNo 实现，智能工厂先行落地** | 代码实测 |
| 8 | 10.4 排期状态 | P0 残留 12 张 org_*/4 重复表、platform 125 表 | **P0/P1 已收尾（残留=0，剩 1 张 legacy 表），platform 90 表** | 生产库实测 |
| 9 | 14 节样板 | 生产系统（服装厂） | **智能工厂（smart-factory）**，样板表 = styles/work_orders 新表 | 老板 8-16 定名 |
| 10 | 9.0 SSO_SECRET/BOT_TOKENS | 待迁 | **进行中：已在 env 文件，待迁纯 env 管理** | 生产实测 |
| 11 | 5.4 示例前缀 | 示例用 genBizNo("RK")（入库单专用，新人照抄会误导） | **改中性示例 genBizNo("WO") + 注释前缀按模块定** | 老板 8-16 意见 |
| 12 | 5.6 落地位置 | 只写统一 genBizNo，未指定封装位置 | **补：封装在 pkg/bizno 全局包，各模块调用，禁止各自实现；新增前缀先查表** | 老板 8-16 意见 |
| 13 | 9.1 access 有效期 | access 15min + refresh 长时 | **access 放宽为 7 天（与 refresh 同为 7 天）——老板 8-16 拍板**；401 自动刷新（/api/refresh 代理 + 前端拦截器）已配套部署 | 老板 8-16 拍板 |

*修订执行：格希欧助理 · 2026-08-16*
