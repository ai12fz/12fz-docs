# 12FZ 用户体系统一规则 v1.0 (草案)

> 统一 ai.12fz.com 与 go.12fz.com 用户体系 · 2026-08-02
> 状态：**草案，待确认**（定案需用户确认后才写入正式文档）

---

## 一、背景与问题

| 现象 | 根因 |
|---|---|
| 好友列表为空（/api/friends/admin 返回空数组） | 前端拿 username("admin") 当查询键，好友实际挂在 uid "1" 下 |
| 设备/Agent 好友关系错乱 | friends 表 user_id 存 text，org_user 是 integer，表示法不统一 |
| 消息路由不到 bridge | 身份标识不一致导致好友/路由链路断裂（已修，见 §六） |

**核心结论**：用户数据已在同一个 PG(12fzsj)，不需要引入外部 IdP。
要做的是**规则统一**，不是系统重建。

---

## 二、现状架构（三服务一库）

```
8.138.235.183 (PG 12fzsj)
├─ public.org_user           用户表（user_id = integer：1=admin、2=测试、11~14=bot）
├─ chat.friends/messages/devices/agents   聊天业务表（user_id = text "1"）
│
├─ go.12fz.com → marketplace 8099
│   ├─ /api/sys/home         身份验证中心（验 token 返回 userInfo）
│   ├─ /sys/* → 8099         管理面板 API
│   └─ /git/* → gitea 3000   代码托管（独立账号体系）
│
└─ ai.12fz.com → chat-token 8081
    ├─ /api/sso/exchange     代理 go.12fz.com/api/sys/home 换本地 JWT
    ├─ /api/auth/whoami      本地 JWT 本地解析 / 其他 token 代理上游
    └─ /ws                   聊天 WebSocket + 设备/Agent 心跳
```

---

## 三、统一规则（六条）

### 规则 1：唯一身份标识 = org_user.user_id（数字字符串）
- 全生态唯一用户键，`ai` / `go` 两域通用
- 任何业务表**禁止**用 username / 昵称 / 手机号当外键
- friends / messages 等表 user_id 一律存数字字符串

### 规则 2：单一签发、本地验证
- marketplace 是唯一登录入口，签发统一 JWT：
  ```json
  { "uid": "1", "username": "admin", "org_id": "", "exp": 0, "iat": 0, "iss": "go.12fz.com" }
  ```
- 密钥放环境变量，chat-token 共享密钥**本地验证**
- 去掉三处代理往返：`internal/handler/sso.go`、`whoami.go`、`http.go:1133`

### 规则 3：当前用户一律从 token 注入
- 所有 `/api/*` 用 AuthMiddleware 解析出的 uid，**不接受** URL 传 userId/username
- 本条直接消灭「好友列表为空」这一类问题

### 规则 4：设备/Agent 是资源身份，不是用户身份
- `d_` token / bot token 保留（设备自己用），payload 必须带 `owner_uid`
- 好友关系（user_type=device/agent）只挂在 owner_uid 名下
- 新设备 / 新 Agent 注册时自动加好友到 owner（已实现：AutoFriendDevice / CreateAgent AddFriend）

### 规则 5：淘汰 session-{uid} 明文 token
- 历史遗留（8/1 P0 已删免验证绕过），下一步不再签发
- 所有登录态统一走规则 2 的 JWT

### 规则 6：Gitea 走 SSO 自动建号（低优先级）
- go.12fz.com/git/ 配 OAuth2 client 对接 marketplace
- 登录一次自动建号，git push 的 SSH key 照旧

### 规则 7：微信登录**强制绑定手机号或邮箱（二选一）** —— 前瞻设计（用户定案 08-02）
**核心策略：手机号/邮箱是跨入口统一锚点**，不依赖 unionid、不依赖微信开放平台认证。
公众号/小程序/网站/未来任何第三方登录，一律映射到「手机号 **或** 邮箱 → user_id」。

1. **登录流程**（微信授权后）：
   ```
   微信授权 → code 换 session（服务端 code2session，openid 不落前端）
   → 查 wx_user_binding 已有绑定？
      有 → 直接签发 JWT
      无 → 强制绑定页（手机号+验证码 / 邮箱+验证码，用户任选其一）：
            phone/email 已存在 org_user → 绑定现有账号并登录（=验证码登录+自动绑微信）
            不存在 → 自动建号（phone 或 email 写入）→ 绑定并登录
   ```
2. **双锚点**：org_user.phone 与 org_user.email 各自唯一约束；
   同一手机号/邮箱多微信（换机/家庭共享）→ 全部绑到同一 user_id，各 openid 互不冲突；
   已绑定用户也可补绑另一项（phone+email 双全，锚点更稳）
3. **绑定表**（新增 ddl）：
   ```sql
   CREATE TABLE wx_user_binding (
     id         UUID PRIMARY KEY,
     user_id    INTEGER NOT NULL REFERENCES org_user(user_id),
     appid      TEXT NOT NULL,          -- 公众号/小程序/网站应用各自 appid
     openid     TEXT NOT NULL,          -- 同一用户不同 appid 下 openid 不同
     created_at TIMESTAMPTZ DEFAULT now(),
     UNIQUE(appid, openid)              -- 一个 openid 只绑一个 user_id
   );
   ```
   （不需要 unionid 字段；如日后接入开放平台可加回）
4. **接口**（走规则 3 token 注入）：
   - `POST /api/auth/wechat/login` — code 换 session + 已绑定则直接登录
   - `POST /api/auth/wechat/bind` — 强制绑定页提交（手机号+短信验证码 或 邮箱+邮件验证码，服务端验证）
   - `POST /api/auth/wechat/unbind`
5. **JWT payload 预留 login_type**（password/wechat/device），本次不实现只留字段位
6. **安全**：state 防 CSRF、code 一次性、验证码频控+图形验证码防刷
7. **前置依赖**：短信通道（阿里云/腾讯云短信）**或** 邮件通道（SMTP/邮件推送）—— 至少其一就位，接入前必须准备；微信开放平台 appid/secret（网站应用，仅需基础认证）

### 规则 8：主机/设备 org 归属——注册码是唯一权威（定案 08-15）
**背景**：曾发生按主机名/IP 猜测归属导致误改（qiuming/gungzhou 机被误判为 M_QIU 测试商户，实际为 ADMIN 平台存量设备）。org 归属是数据决策，**禁止推断**。

1. **新设备**：唯一合法途径是「生成注册码 → 设备用码注册」。
   - 生成端强制 org_id：`POST /api/hosts/reg-code` 必须带 `org_id`（admin 生成时必须显式指定商户，空 org 直接 400）
   - 消费端 `RegisterDevice` 用注册码兑换 `host_reg_codes.org_id` 写入 `hosts.org_id`，设备永远落不到空 org
2. **存量设备**：注册码机制上线前注册的主机 org_id 一律为 `00000000-0000-0000-0000-000000000000`（ADMIN 平台），**不得**按主机名/IP/别名推断归属
3. **改归属**：任何人要改 `hosts.org_id` / `proxy_keys.org_id`，先查该主机的注册来源（`host_reg_codes`、创建时间、生成人），来源不明时**先问平台主，再动手**
4. **判定顺序**：注册码绑定 > 创建记录 > 询问平台主；名字/昵称/IP **不是**归属依据

### 规则 8.5：归属变更执行机制（定案 08-15）
**背景**：曾发生归属被直接连库修改且无人知情。规则 8 需要执行手段兜底——改归属可追溯、漂移自动发现。
1. **合法途径**：改 `hosts.org_id` / `proxy_keys.org_id` 只有两条合法途径：① 注册码流程（RegisterDevice，代码内建）；② 归属变更管理接口（规划中，上线后替代手工方式）。**禁止直接连库 UPDATE org 字段**。
2. **审计留痕**：所有 org 变更自动记录：变更人身份（DB 角色/应用来源）、时间、客户端 IP、旧值→新值——**一律可追溯**。
3. **每日核对**：系统每日自动核对「注册码 org vs 主机 org vs key org」，不一致即告警。
4. **追责**：绕过合法途径直接连库改归属属违规操作，审计记录作为追责依据。

---

## 四、落地计划

| 优先级 | 事项 | 涉及 |
|---|---|---|
| **P0** | GetFriends 忽略 URL 的 user_id，改用 token 里的 uid | chat-token http.go |
| **P0** | 前端 loadFriends 删掉 session- 分支，统一走 whoami.user_id | SidebarLeft.vue |
| **P1** | marketplace 改签标准 JWT；chat-token 本地验签，删三处代理 | marketplace / chat-token |
| **P2** | 设备/Agent token 加 owner_uid；好友表按 owner 挂接 | chat-token |
| **P2(预留)** | wx_user_binding 表 + wechat login/bind/unbind 接口（前置：短信通道 + 微信开放平台 appid/secret） | marketplace / ddl |
| **P3** | Gitea OAuth2 + 用户表 schema 整理（归 iam schema） | gitea / ddl |

---

## 五、跨服务接口规范

| 场景 | 规范 |
|---|---|
| 用户身份传递 | 一律 `user_id`（数字串），禁止 username |
| 登录态 | 统一 JWT（规则 2 payload） |
| 设备/Agent 身份 | `d_`/bot token + `owner_uid` |
| 好友关系 | `friends(user_id=owner, friend_id=设备/Agent/用户, user_type)` |

---

## 六、本次事故处理记录

- **WS 断连循环**：bridge 36s 断连已定位排除（复现实验证明链路正常，重启后 90s 零断开，设备 online）
- **好友列表为空**：前端查 `friends/admin` vs 数据在 `friends/1`，待 P0 修复

---

## 七、版本记录

| 版本 | 日期 | 内容 |
|---|---|---|
| v1.5 | 08-15 | 新增规则 8.5：归属变更执行机制（审计留痕/每日核对/禁直连改库） |
| v1.4 | 08-15 | 新增规则 8：主机/设备 org 归属——注册码唯一权威，禁止按名字/IP 推断 |
| v1.3 | 08-02 | 规则 7 补充定案：手机/邮箱二选一，双锚点各自唯一 |
| v1.2 | 08-02 | 规则 7 定案：微信登录强制绑定手机号，手机号为跨入口唯一锚点 |
| v1.1 | 08-02 | 新增规则 7：微信登录绑定规范（前瞻设计） |
| v1.0 | 08-02 | 草案：六条规则 + P0~P3 落地计划 |

## 相关文档

- [12FZ Marketplace 开发文档 v3.29](12FZ Marketplace 开发文档 v3.29.md)
- [12FZ智能中台-超级平台v3.28.md](12FZ智能中台-超级平台v3.28.md)
- [PROJECT_MEMORY.md](PROJECT_MEMORY.md)
- [ARCHITECTURE.md](ARCHITECTURE.md)
