# 12FZ 安全规范 v1.0（定案版）

> 配套《12FZ 统一身份与命名规范 v3.1》· 2026-08-15
> 原则：安全是重点——认证/授权/传输/数据/密钥/审计/接口/运维九维全覆盖，一次落地

---

## 一、认证安全

| 项 | 规范 |
|---|---|
| **密码存储** | bcrypt（cost≥10）或 argon2id；禁止明文/MD5/SHA1 存储 |
| **JWT** | HS256（密钥≥32 字节随机）或 RS256；**短时效 2h**；payload 只含 `{uid, tenant_id, roles, exp, iat}`——不装敏感信息 |
| **Refresh Token** | 长时效（30 天）+ 轮换 + 服务端吊销列表（账号禁用即时生效） |
| **登录防暴力** | 同账号/IP 失败 5 次→验证码；10 次→锁定 15 分钟（Redis 计数） |
| **微信登录** | code 一次性 + state 防 CSRF + openid 不落前端（规则 7 延续） |
| **会话** | 彻底淘汰 `session-` 明文 token（本次迁移禁用） |

## 二、授权安全

| 项 | 规范 |
|---|---|
| **RBAC** | iam.roles/permissions/user_tenants 全量落地；权限中间件 fail-closed（无角色=拒绝） |
| **租户隔离** | L1 中间件（RequireTenant）→ L2 三维度过滤（tenant_id + industry + menu）→ L3 RLS（41 表 FORCE）——三层全开 |
| **敏感操作二次验证** | 改归属 / 删除数据 / 大额充值（>¥1000）→ 二次确认（密码 or 验证码） |
| **admin 豁免** | 仅 super_admin（uid=1 + 平台租户）可跨租户；中间件硬编码校验，禁止前端判断 |

## 三、传输安全

| 项 | 规范 |
|---|---|
| **HTTPS 全站** | go.12fz.com / ai.12fz.com 强制 TLS 1.2+；HSTS；证书自动续期 |
| **禁止** | 明文 HTTP 业务流量；内部服务（8099/8081）只监听 127.0.0.1 或内网 |

## 四、数据安全

| 项 | 规范 |
|---|---|
| **敏感字段加密** | `proxy_keys.key_text`、设备 token、API secret——应用层 AES-GCM 加密存储（密钥独立管理） |
| **PII 脱敏** | 手机号/邮箱展示脱敏（138****1234）；日志禁止落明文 PII |
| **备份安全** | pg_dump 产物加密（gpg）或存私有盘；保留 14 天；恢复演练 |
| **软删除** | 统一 deleted_at——物理删除仅运维授权（防误删） |
| **最小数据留存** | 敏感日志（登录失败/越权尝试）留 90 天 |

## 五、密钥管理

| 项 | 规范 |
|---|---|
| **配置来源** | JWT 密钥 / DB DSN / API key 一律环境变量或密钥文件（0600）——禁止写死代码/明文配置文件 |
| **密钥轮换** | JWT 密钥季度轮换（双密钥平滑切换）；`sh-` 商户 key 支持吊销重发 |
| **访问控制** | 服务器文件权限最小化；.git 不含 .env/密钥 |

## 六、审计与监控

| 项 | 规范 |
|---|---|
| **审计** | `iam.audit_log` 全量记录：身份/归属/权限变更、登录成功/失败、敏感操作（actor_uid/ip/app/时间/新旧值） |
| **访问日志** | 全接口 access log（uid/tenant_id/耗时/状态）——本次 API 规范落地 |
| **异常告警** | 登录失败突增 / 越权尝试（403 密集）/ 计费异常（余额突减/402 复发）/ 漂移检测——告警进日志+通知 |
| **日志安全** | 日志脱敏（无明文 key/PII）；日志权限受控 |

## 七、接口安全

| 项 | 规范 |
|---|---|
| **限流** | 令牌桶 10000/s（已有）+ 按 uid/ip 二级限流（写接口更严） |
| **CORS** | 白名单（仅 go.12fz.com/ai.12fz.com）；禁止 `*` |
| **CSRF** | 写操作校验 Origin/Referer + SameSite Cookie |
| **输入校验** | 全参数化查询（pgx 已保证防注入）；长度/类型/枚举服务端校验（不信任前端） |
| **幂等** | 写操作 Idempotency-Key（防重复扣费/下单） |
| **文件上传** | 类型白名单 + 大小限制 + 存储隔离（不可执行目录） |

## 八、运维安全

| 项 | 规范 |
|---|---|
| **DB 最小权限** | app_zhongtai 拆分为：`svc_billing`（只读写计费域）/ `svc_biz`（业务域读写）/ `svc_readonly`（报表）——禁止单一超权角色 |
| **角色分离** | marketplace/chat-token 各用独立 DB 角色 + 独立密钥 |
| **SSH/端口** | 只留必要端口（22 白名单 IP）；内部端口不暴露公网 |
| **依赖漏洞** | go vulncheck / npm audit 定期扫描；Gitea 安全公告跟进 |
| **补丁** | 系统/数据库/Gitea 安全补丁月度例行 |

## 九、安全测试（迁移验收含）

- [ ] 认证：暴力破解防护 / JWT 伪造 / 过期 / 吊销测试
- [ ] 授权：越权访问（跨租户/跨角色）测试——隔离 8/12 回归 + RBAC 矩阵
- [ ] 注入：SQL 注入/XSS/CSRF 扫描（sqlmap/zap 基础扫描）
- [ ] 传输：TLS 配置检查（SSL Labs）
- [ ] 敏感数据：全库扫描无明文密码/密钥（正则扫描 key_text/secret 落库检查）
- [ ] 审计：敏感操作留痕抽查
- [ ] 备份：恢复演练（克隆库还原验证）

---

## 十、与本次迁移的绑定

- 安全规范**全部纳入本次迁移**（原则：零技术债——安全债更是债）
- 迁移计划阶段 3/6 增加：密码哈希升级（如存量明文→bcrypt）、key 加密存储、DB 角色拆分、敏感操作二次验证、异常告警
- 验收清单增加第九节安全测试项

---

*安全规范 v1.0 · 2026-08-15 · 定案版，纳入迁移实施*
