# 12FZ 系统架构与用户体系规范（总纲）v4.0

> 全系统唯一权威规范 · 2026-08-15
> v4.0 = 总纲 v2.0 + 高级工程师《身份与命名重构方案 v1.0》（实测核实）整合
> 原则（老板定案）：**彻底改进、零技术债、不考虑工作量**；代码质量与稳定性扩展性优先
> 配套：《12FZ Marketplace 开发文档 v3.35》（业务详档）

---

## 〇、系统全景

### 0.1 域名分工（三入口一库）

| 域名 | 用途 | 后端 |
|---|---|---|
| **go.12fz.com** | 业务 API + SPA（Marketplace） | Go 8099 |
| **go.12fz.com/git/** | Gitea（OAuth2 对接 iam，最后阶段） | Gitea 3000 |
| **ai.12fz.com** | 聊天 WS + 主机/Agent 心跳 | chat-token 8081 |
| **ai.12fz.com/v1** | 大模型中转（OpenAI 兼容） | chat-token 8081 |

**统一数据底座**：PostgreSQL 12fzsj（public 157 表 + chat 31 表）+ Redis 7。`uid`（BIGINT）与 `tenant_id`（UUID）贯穿全系统。

### 0.2 两域关系

```
go.12fz.com（业务/管理门户）          ai.12fz.com（聊天/AI 服务）
  唯一登录入口，签发 JWT ───────────►  公钥本地验签（RS256）
  用户/商户/订单/账本                  聊天 WS + 设备心跳 + /v1 中转计费
  共用 12fzsj 库，同一 uid/tenant_id
```

### 0.3 标识前缀

| 类型 | 前缀 |
|---|---|
| 中转商户 key | `sh-` |
| Agent token | `st-` |
| 设备密钥 | `fz-` |
| 设备 ID | `d_` |
| 注册码 | `zc-` |

---

## 一、用户体系规则

### 规则 1：唯一身份标识 = `uid`（BIGINT）
- 全生态唯一用户键；禁止 username/昵称/手机号当外键
- **值不重排**：1=admin、25=suzao、26=qiuming 原样继承（org_user 实测仅 3 行）
- **agent/host 统一映射进 uid 域**（bot 11-14 先例；host 分配"资源型 uid"）——消息/好友/权限全按 uid 处理

### 规则 2：单一签发、本地验证
- marketplace 唯一登录入口，签发 **RS256 非对称 JWT** `{uid, tenant_id, roles}`
- 私钥仅 SSO 签发方；公钥分发 chat-token 本地验签；**JWKS + kid 支持轮换/吊销**
- **access token 15min + refresh token 长时**分离

### 规则 3：当前用户一律从 token 注入
- 所有 `/api/*` 用中间件解析的 uid；不接受 URL 传 userId/username

### 规则 4：设备/Agent 是资源身份
- `d_`/`st-` token 保留（设备自用），payload 带 `owner_uid`；好友挂 owner 名下

### 规则 5：登录账号（accounts 模型）
- `iam.accounts`（login_type: password/phone/email/wechat + identifier 唯一）；一用户多登录方式
- **users 保留 phone/email 冗余展示字段，accounts 为权威——写触发器在 accounts 变更时同步**
- 微信登录：手机号/邮箱二选一为跨入口锚点

### 规则 6：主机/设备归属——注册码是唯一权威
1. 新设备唯一合法途径：生成注册码（`zc-`，强制 tenant_id，空 400）→ RegisterDevice 兑换归属
2. 存量设备一律平台（`00000000-...`）；禁止按名字/IP 推断
3. 改归属：先查注册来源（host_reg_codes/创建时间/生成人），不明先问平台主；禁止直接连库改
4. 判定顺序：注册码绑定 > 创建记录 > 询问平台主

### 规则 7：归属变更执行机制
1. 合法途径：注册码流程 / 归属变更接口；禁止直接连库 UPDATE
2. 审计：`iam.audit_log` append-only 全量记录（actor_uid/ip/app/时间/新旧 JSONB）
3. 每日漂移核对（注册码 tenant vs 主机 tenant vs key tenant）——告警
4. 追责：审计记录为依据

### 规则 8：商户数据隔离三维度
tenant_id + industry_code + menu_ids；四条铁律（服务端解析/fail-closed/读写全覆盖/admin 豁免）；三层防御（RequireTenant → 三维度 → RLS 41 表 FORCE + AfterConnect 默认 admin + Scoped 事务覆盖）

---

## 二、命名与通用规范

### 2.1 术语

| 术语 | 含义 | 禁止 |
|---|---|---|
| `uid` | 用户 ID（BIGINT，含资源型 uid） | ~~user_id~~、username 外键 |
| `tenant_id` | 租户/组织 ID（UUID） | ~~org_id~~ |
| `account_id` | 登录账号 ID | — |
| `owner_uid` / `creator_uid` | 操作人 | — |

### 2.2 Schema 分层（三 schema，决策 5 定稿=方案 A 彻底）

```
iam.*        身份域（users/accounts/tenants/roles/permissions/role_permissions/user_tenants/audit_log）
chat.*       聊天域（contacts/user_agents/user_hosts/messages/hosts/agents/proxy_*）
platform.*   业务域（orders/products/finance_ledger...——去 platform_ 前缀，废弃表直接 DROP 不进）
```

- 91 张表 org_id → tenant_id（86 uuid 纯改名 + 4 text 合法 uuid `USING ::uuid` + mind_map 空串→00000000 平台）
- 30 张表 user_id → uid（5 种类型 bigint/integer/text/uuid/varchar 统一 BIGINT；脏值：documents 13 个、friends 1 个——迁移前清单逐个归属，无法归属置 NULL + 告警）
- 代码引用实测：marketplace 851 + chat-token 135 + auth-service 5 ≈ **991 处**

### 2.3 通用规范
- 审计字段 created_at/updated_at；软删除 deleted_at；外键显式+索引；JSONB；枚举 TEXT+CHECK；TIMESTAMPTZ
- API：`/api/v1/`；响应 `{code,message,data}`；错误码 0/40001/40100/40300/40400/42900/50000；幂等 Idempotency-Key；access log 无敏感数据

---

## 三、安全规范（本次重构一并落实）

### 3.1 认证
- **废弃 session- 明文 token**（最大安全债）；JWT **RS256** + JWKS/kid 轮换吊销；access 15min + refresh 长时
- 密码迁移统一升级 **bcrypt/argon2**（不保留弱哈希）；登录防暴力（5 次验证码/10 次锁定）
- 微信 code 一次性 + state 防 CSRF

### 3.2 授权
RBAC 中间件 fail-closed（越权 40300）；RLS 按 tenant_id 强制行级隔离（防 IDOR/跨租户）；switch-tenant 校验归属（否则 403）；敏感操作二次验证（改归属/删数据/大额充值>¥1000）；admin 豁免仅 super_admin

### 3.3 数据
`chat.agents.api_key` 等密钥应用层加密存储（不落明文）；PII（手机/邮箱/openid）审计日志脱敏；备份加密留 14 天；软删除统一

### 3.4 审计
`iam.audit_log` **append-only**（业务账号无 UPDATE/DELETE 权限）；覆盖身份/归属/权限变更（actor_uid + old/new JSONB）

### 3.5 迁移过程
pg_dump 后完整性校验（verify + 行数/值断言）；迁移脚本最小权限运行、日志不输出敏感值；**停机窗口维护模式阻断外部访问**；HTTPS 全站；token/密码/openid 不进 access log

### 3.6 运维
DB 角色拆分（svc_billing/svc_biz/svc_readonly）；服务独立角色密钥；端口收敛；依赖漏洞扫描

---

## 四、技术债清理清单（一次性，零过渡层）

| # | 债 | 处置 |
|---|---|---|
| 1 | public.org_user（3 行） | 迁 iam.users/accounts 后 DROP |
| 2 | org_id 列（91 表） | 全改 tenant_id |
| 3 | user_id 列（30 表，5 类型） | 全转 BIGINT uid |
| 4 | chat.friends 兼容视图 / friends_bak / friends_old | DROP |
| 5 | chat.host_codes / chat.capabilities 废弃项 | DROP |
| 6 | JWT payload username / session- token | 移除/彻底禁用 |
| 7 | 三处认证代理（sso/whoami/http.go:1133） | 删除，RS256 本地验签 |
| 8 | auth-service（8098） | 已废弃归档，不启 |
| 9 | org_change_audit 专用表 | 并入 iam.audit_log |
| 10 | 单一超权 DB 角色 | 拆 svc_* |
| 11 | RBAC 预留 | 完整落地 |
| 12 | Gitea 独立账号 | OAuth2 对接 iam（最后阶段） |

---

## 附录：迁移实施计划（一次性·执行后归档）

### 阶段 0 — 准备（不碰生产，✅ 已部分完成）
- [x] 全库备份：`/opt/backup/12fzsj_pre_iam_20260815_145248.dump`（108MB，14:52）
- [x] 迁移脚本：/tmp/iam-migration/001-iam-schema.sql ~ 005-cleanup.sql（iam_test 克隆库演练通过）
- [x] 现状盘点：91 表 org_id / 30 表 user_id 5 类型 / 504 万行 proxy_usage / 脏值清单 / 991 处代码引用
- [ ] 建迁移分支；代码重构（marketplace + chat-token）**分支全量完成、编译通过**
- [ ] 脚本在备份还原临时库试跑（列/行/值一致性断言）
- [ ] cron 暂停（归档/漂移/pg_dump）；维护模式预案；停机通知（Hermes/202/Windows/设备/gxo 下单支付联动）

### 阶段 1-5（停机窗口，预估 60-90 分钟——代码预完成）
1. **DDL**：iam schema 全套；91 表 org_id→tenant_id；30 表 user_id→uid；RLS 41 表按 tenant_id；audit 触发器
2. **数据迁移**：org_user(3 行)→users/accounts（uid 不变、salt+password→secret_hash、wechat_openid→accounts）；org_merchant(3 行)→tenants；org_user.org_id→user_tenants(is_default)；脏值处理；setval 同步
3. **代码+部署**：marketplace/chat-token 新二进制+前端；JWT RS256 切换；session- 删除；三处代理删除
4. **清理**：DROP org_user/friends_*/host_codes/兼容视图/capabilities；org_change_audit 并入
5. **回归**：登录（账号/微信/手机）、聊天（messages sender_uid）、中转计费（504 万行 proxy_usage）、商户隔离（平台 12/商户 8）、设备注册、RBAC 矩阵、安全测试

### 验收标准（全部达标）
- [ ] 全库无 org_id/user_id 列名；无 session-；无三处代理；无弱哈希
- [ ] iam.audit_log append-only + PII 脱敏；记录全部身份/归属变更
- [ ] RLS 按 tenant_id 隔离（平台 12/商户 8）+ 越权测试（跨租户 403）
- [ ] RBAC super_admin/merchant_admin 矩阵通过
- [ ] 计费单价（官方×2、高峰×2）回归通过
- [ ] BIGSERIAL setval 正确；RS256 + 轮换生效；旧表全部 DROP 零过渡层

### Gitea OAuth2（独立最后阶段）
OAuth2 client 对接 iam——git 登录自动建号（accounts.login_type=oauth）

### 回滚预案
代码分支预完成降低窗口风险；每阶段 checkpoint；pg_dump 备份可 pg_restore 回滚；旧表验收通过前不 DROP；session- 切换可选 72h 双 token 兼容期

---

## 版本记录

| 版本 | 日期 | 内容 |
|---|---|---|
| v4.0 | 08-15 | 整合高工方案：agent/host 入 uid 域、phone 冗余+触发器、三 schema、RS256/15min、60-90min 窗口、6 阶段、实测盘点（91 表/991 处/备份） |
| v2.0 | 08-15 | 总纲合并（规则 v1.5 + 身份规范 v3.1 + 安全规范 v1.0 + 迁移计划 v1.0） |
| v1.5 | 08-15 | 规则 8.5：归属变更执行机制 |
| v1.0 | 08-02 | 六条规则草案 |

---

*总纲 v4.0 · 2026-08-15 · 整合定案版（含高级工程师方案），待确认后进入阶段 0 剩余项*
