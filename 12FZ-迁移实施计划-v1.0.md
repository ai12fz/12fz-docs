# 12FZ 迁移实施计划 v1.0

> 依据《12FZ 统一身份与命名规范 v3.1》· 一次停机窗口 · 2026-08-15
> 原则：彻底、零技术债、不拆分——2-4 小时停机（含回滚预案）

---

## 阶段 0：预检（停机前 1-2 天完成，不停机）

- [ ] 全库备份：`pg_dump -Fc`（含归档表 proxy_usage_archive）
- [ ] 脏值扫描：
  - chat.documents 13 个非数字 user_id、friends 1 个非数字——列清洗清单
  - 5 张 text/varchar org_id 表（86 uuid + 4 text + 1 varchar）——逐值校验合法性
  - 非法值处理：`0`/空/`00000000`→平台；其他→人工清单
- [ ] proxy_usage 504 万行迁移策略确认（加列-分批-换列）
- [ ] 停机窗口确认（业务低峰/工作日夜间）+ 通知相关方
- [ ] 迁移脚本 dry-run 于克隆库全量演练

## 阶段 1：DDL（停机 T+0）

1. 建 `iam` schema 全套表（users/accounts/tenants/user_tenants/roles/permissions/role_permissions/audit_log）
2. 业务表（41 个 platform_）：`org_id` → `tenant_id`（RENAME COLUMN + 类型校验）
3. 聊天表：`user_id` text → `uid` BIGINT（含 messages 改 sender_type+sender_id）
4. `chat.messages` 模型改造：`sender_id` + `sender_type(user/agent/host)`（存量按来源回填类型）
5. proxy_usage：加 `uid BIGINT` 列 → 分批 UPDATE（500 万行，批 10 万/次）→ 建索引 → 换列

## 阶段 2：数据迁移（停机 T+1）

1. `org_user`（3 行：1/25/26）→ `iam.users`（uid 值不变）+ `iam.accounts`（手机/邮箱拆出）
2. 建 `iam.tenants`（00000000 平台 + 3fc9b875 测试商户等）+ `iam.user_tenants`（按现有绑定）
3. 角色初始化：super_admin（uid=1→tenant 平台）、merchant_admin/staff 种子数据
4. 业务表 owner_uid：存量置 NULL（不编造）
5. 数据校验：逐表数量一致 + uid/tenant_id 值一致（校验脚本）

## 阶段 3：代码重构（与阶段 1/2 并行开发，停机时切换）

**chat-token（12fzwebsocket）**
- [ ] 全部 SQL：user_id→uid、org_id→tenant_id
- [ ] JWT：payload `{uid, tenant_id, roles}`；本地验签；删 sso.go/whoami.go/http.go:1133 三处代理
- [ ] 删 session- 分支（SSO 停签 + AuthMiddleware 拒绝）
- [ ] messages 读写改 sender_type/sender_id
- [ ] 中间件：RequireOrg 改为 RequireTenant（JWT tenant_id + 绑定校验）

**marketplace（8099）**
- [ ] 全部 SQL/ORM 适配 uid/tenant_id
- [ ] RequireOrg 中间件 → RequireTenant（fail-closed）
- [ ] API 响应结构 `{code,message,data}` + 错误码表（327+ 路由）
- [ ] RBAC：roles/permissions/user_tenants + 权限中间件（super_admin/merchant_admin/staff）
- [ ] switch-tenant 接口（POST /api/v1/auth/switch-tenant → 刷新 token）

**前端**
- [ ] 全量 API 适配（响应结构/字段名）
- [ ] 多租户切换 UI（当前租户显示 + 切换入口）
- [ ] loadFriends 等走 uid（无 session- 分支）

**Gitea**
- [ ] OAuth2 client 对接 iam（规则 6）：登录一次自动建号（accounts.login_type=oauth）

## 阶段 4：依赖更新（停机 T+2）

- [ ] RLS 策略：41 表 `org_id` → `tenant_id`（策略条件同步）
- [ ] 审计触发器：org_change_audit → 并入 `iam.audit_log`（通用审计：tbl/row_id/action/old/new/actor_uid/ip/app_name）
- [ ] 漂移检测脚本：核对「注册码 tenant vs 主机 tenant vs key tenant」
- [ ] 计费防护触发器（migration_v4）列名同步

## 阶段 5：技术债清理（停机 T+2）

- [ ] DROP `public.org_user`（迁移后）
- [ ] DROP `chat.friends` 兼容视图、`friends_bak_20260802`、`friends_old`
- [ ] DROP `chat.host_codes`（空表）
- [ ] 删除 `session-` token 相关代码/数据
- [ ] 8098 auth-service：确认已停（已归档）——不启

## 阶段 6：功能落地（停机 T+2~T+3）

- [ ] API 规范全量生效（新错误码 + 前端适配完成）
- [ ] RBAC 权限矩阵测试（super_admin 全量 / merchant_admin 商户域 / staff 受限）
- [ ] 多租户切换链路（switch-tenant + JWT 刷新 + 中间件绑定校验）
- [ ] Gitea OAuth2 登录链路（git push 走 SSO 建号）

## 阶段 7：全量回归（停机 T+3，验收清单）

- [ ] 登录：密码/手机/微信（accounts 多方式）——uid 贯穿
- [ ] 聊天：WS + messages（sender 多态）+ 好友（三表）+ 群聊
- [ ] 中转/计费：key 鉴权（sh-）、扣费、单价验证（官方×2、高峰×2）
- [ ] 商户隔离：平台 12 单 / 商户 8 单（RLS 按 tenant_id）——回归
- [ ] 设备：注册码注册 → 归属 → 心跳 → 调用（规则 8）
- [ ] 权限：RBAC 矩阵
- [ ] 审计：audit_log 记录归属/身份变更
- [ ] 漂移检测：无漂移

## 阶段 8：文档同步（停机后）

- [ ] 用户体系规则 → v2.0（uid/tenant_id 术语 + 规则 9/10 新增）
- [ ] Marketplace 开发文档 → v3.36
- [ ] 迁移记录归档（本次实施文档留存）

---

## 回滚预案

| 场景 | 动作 |
|---|---|
| DDL 失败 | 阶段 1 前全库备份恢复（30 分钟内回滚到停机前） |
| 数据迁移校验不过 | 停止阶段 2，恢复备份；保留 iam 表重建 |
| 代码切换后核心功能异常 | 回滚代码到迁移前 commit + 恢复备份（旧表保留至验收通过再 DROP） |
| 验收通过 | DROP 旧表不可逆操作放在停机窗口最后，验收后执行 |

**安全边界**：旧表（org_user/friends_*）在**验收通过前不 DROP**——DROP 是最后一步，不可逆操作全部放窗口末尾。

---

*迁移实施计划 v1.0 · 2026-08-15 · 待实施*
