# 12FZ 统一身份与命名规范 v3.1（定案版·全量范围）

> 未上线窗口一次性彻底重构 · 2026-08-15
> 原则（老板定案，不变）：**彻底改进、零技术债、不拆分、不考虑工作量**
> 范围：命名重构 + 表结构 + 数据迁移 + 技术债清理 + API 规范 + RBAC + Gitea SSO + 多租户切换——**一次停机窗口全部完成**

---

## 一、设计原则（大厂行业惯例）

1. **身份域独立**：用户/账号/租户/权限归 `iam` schema，与业务域解耦
2. **uid 全局唯一**：用户 ID 用 `uid`（BIGINT），彻底取代 `user_id`
3. **租户模型**：组织/商户统一 `tenant_id`（UUID），取代泛义 `org_id`
4. **账号与用户分离**：`accounts`（登录凭证）独立于 `users`（身份）
5. **归属双维度**：业务数据统一 `tenant_id`（哪个租户）+ `owner_uid`（哪个用户）
6. **uid / tenant_id 值不重排**：现有值原样继承（1=admin、00000000=平台）——历史引用不断
7. **零技术债**：废弃表/兼容视图/代理/旧 token 一次性清理，不留过渡层

---

## 二、设计依据（为什么这么选）

| 决策 | 依据 |
|---|---|
| `uid`（用户 ID） | user_id 与 org_id 一字之差是混淆根源；uid 是行业惯例（微信/游戏/各大平台）；chat 表 text/int 类型混乱需一次性统一为 BIGINT |
| `tenant_id`（租户 ID） | org 语义太泛（组织/机构/商户/公司都叫 org）；`org_user` 表名误导；大厂多租户 SaaS 标准词是 tenant（阿里云/飞书/钉钉）；商户=租户一种，扩展天然清晰 |
| `iam` schema | 身份/业务/聊天/中转分域职责清晰；标准 RBAC 打底；大厂中台统一 IAM 惯例 |
| `accounts` 分离 | 一用户多登录方式（密码/手机/微信/邮箱）凭证独立；密码哈希/登录审计独立管理；规则 7 双锚点（手机/邮箱）天然落位 |
| uid 值不重排 | 1=admin、2=测试、11-14=bot 原样继承——历史数据/外部引用不断——迁移可验证 |
| 现在做 | Marketplace 未上线、商户未入驻——纯内部改动无客户影响；上线后改成本 10 倍+；已有混淆改错先例（08-15 归属误判）需根因治理 |

---

## 三、目标表结构（字段级完整定义）

### 3.1 身份域（iam schema——新建）

```sql
-- 用户主表
CREATE TABLE iam.users (
  uid          BIGSERIAL PRIMARY KEY,           -- 用户 ID（继承 org_user.user_id 值）
  username     TEXT,                            -- 可选别名（仅展示，禁止当外键）
  nickname     TEXT,
  avatar       TEXT,
  phone        TEXT,                            -- 冗余展示用，权威在 accounts
  email        TEXT,
  status       TEXT NOT NULL DEFAULT 'active',  -- active/disabled
  created_at   TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at   TIMESTAMPTZ NOT NULL DEFAULT now(),
  deleted_at   TIMESTAMPTZ                      -- 软删除
);

-- 登录账号（一用户多登录方式）
CREATE TABLE iam.accounts (
  id           BIGSERIAL PRIMARY KEY,
  uid          BIGINT NOT NULL REFERENCES iam.users(uid),
  login_type   TEXT NOT NULL,                   -- password/phone/email/wechat
  identifier   TEXT NOT NULL,                   -- 手机号/邮箱/openid/用户名
  secret_hash  TEXT,                            -- 密码哈希（wechat 空）
  status       TEXT NOT NULL DEFAULT 'active',
  created_at   TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at   TIMESTAMPTZ NOT NULL DEFAULT now(),
  UNIQUE (login_type, identifier)
);

-- 租户/组织（商户 = type='merchant'）
CREATE TABLE iam.tenants (
  tenant_id    UUID PRIMARY KEY DEFAULT gen_random_uuid(),  -- 平台=00000000-... 值继承
  name         TEXT NOT NULL,
  type         TEXT NOT NULL,                   -- platform/merchant/company
  status       TEXT NOT NULL DEFAULT 'active',
  created_at   TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at   TIMESTAMPTZ NOT NULL DEFAULT now(),
  deleted_at   TIMESTAMPTZ
);

-- 用户-租户关系（RBAC 挂点）
CREATE TABLE iam.user_tenants (
  id           BIGSERIAL PRIMARY KEY,
  uid          BIGINT NOT NULL REFERENCES iam.users(uid),
  tenant_id    UUID NOT NULL REFERENCES iam.tenants(tenant_id),
  role_id      BIGINT NOT NULL REFERENCES iam.roles(role_id),
  is_default   BOOLEAN NOT NULL DEFAULT false,
  created_at   TIMESTAMPTZ NOT NULL DEFAULT now(),
  UNIQUE (uid, tenant_id)
);

-- 角色权限（标准 RBAC）
CREATE TABLE iam.roles (
  role_id      BIGSERIAL PRIMARY KEY,
  code         TEXT NOT NULL UNIQUE,            -- super_admin/merchant_admin/staff
  name         TEXT NOT NULL,
  is_system    BOOLEAN NOT NULL DEFAULT false,
  created_at   TIMESTAMPTZ NOT NULL DEFAULT now()
);
CREATE TABLE iam.permissions (
  perm_id      BIGSERIAL PRIMARY KEY,
  code         TEXT NOT NULL UNIQUE,            -- order:read / order:write ...
  name         TEXT NOT NULL,
  created_at   TIMESTAMPTZ NOT NULL DEFAULT now()
);
CREATE TABLE iam.role_permissions (
  role_id      BIGINT NOT NULL REFERENCES iam.roles(role_id),
  perm_id      BIGINT NOT NULL REFERENCES iam.permissions(perm_id),
  PRIMARY KEY (role_id, perm_id)
);
```

### 3.2 聊天域（chat schema——uid 统一 BIGINT）

```
chat.contacts      (id, uid BIGINT, contact_uid BIGINT, created_at, UNIQUE(uid, contact_uid))
chat.user_agents   (id, uid BIGINT, agent_id TEXT, created_at, FK)
chat.user_hosts    (id, uid BIGINT, host_id TEXT, created_at, FK)
chat.messages      (id, sender_uid BIGINT, receiver_uid BIGINT, content, created_at, deleted_at)
chat.groups        (id, name, tenant_id UUID, created_at)
chat.group_members (group_id, uid BIGINT, role, created_at)
chat.hosts         (id, name, tenant_id UUID, owner_uid BIGINT, status, last_seen, ...)
chat.agents        (id, name, tenant_id UUID, owner_uid BIGINT, status, ...)
chat.proxy_keys    (key_id, key_text, tenant_id UUID, host_id, uid BIGINT, status, created_at, revoked_at)
chat.proxy_usage   (id, tenant_id UUID, uid BIGINT, key_id, model_name, prompt_tokens, comp_tokens, cost, created_at)
chat.host_reg_codes(code, tenant_id UUID, host_id, status, created_by BIGINT, used_at, expires_at)
```

### 3.3 业务域（platform schema——全部 tenant_id 列名）

```
platform_orders            (order_id, tenant_id UUID, owner_uid BIGINT, amount, status, ...)
platform_finance_ledger    (id, tenant_id UUID, owner_uid BIGINT, biz_type, direction, amount_cny, status, ...)
platform_products          (product_id, tenant_id UUID, ...)
... 41 个 platform_ 表：org_id → tenant_id，新增 owner_uid 统一
```

### 3.4 中转域（proxy——tenant_id + uid）

```
chat.proxy_keys / chat.proxy_usage / chat.proxy_pricing
（见 3.2——key/usage 带 tenant_id + uid；pricing 全局定价不变）
```

---

## 四、通用规范（全库强制）

| 规范 | 规则 |
|---|---|
| **审计字段** | 每表 `created_at`/`updated_at` TIMESTAMPTZ NOT NULL DEFAULT now() |
| **软删除** | `deleted_at TIMESTAMPTZ`；查询默认 `WHERE deleted_at IS NULL`；物理删除仅运维授权 |
| **ID 生成** | 用户 `BIGSERIAL`（继承值）；租户/业务 UUID `gen_random_uuid()`；不暴露自增序列给客户端 |
| **命名** | 小写下划线；表复数；`{域}_` 前缀（iam./platform./chat.）；禁止缩写混用 |
| **外键** | 显式 FK + 索引；禁止无 FK 的孤儿引用 |
| **通用审计表** | `iam.audit_log(id, tbl, row_id, action, old_val JSONB, new_val JSONB, actor_uid, actor_ip, app_name, created_at)`——org_change_audit 并入 |
| **JSONB 字段** | 业务扩展用 JSONB，禁止 EAV 表 |
| **时区** | 一律 TIMESTAMPTZ（UTC 存储，展示时区转换） |
| **枚举** | TEXT + CHECK 约束（禁魔法数字） |

---

## 五、技术债清理清单（一次性消灭，零过渡层）

| # | 技术债 | 处置 |
|---|---|---|
| 1 | `public.org_user` | 数据迁 iam.users/accounts 后 **DROP** |
| 2 | `org_id` 列（41+ 表） | 全改 `tenant_id` |
| 3 | chat.*.user_id 存 text | 全转 BIGINT `uid` |
| 4 | `chat.friends` 兼容视图 | 删除——三表直接使用 |
| 5 | `chat.friends_bak_20260802` / `friends_old` | **DROP** |
| 6 | `chat.host_codes`（空表） | **DROP** |
| 7 | JWT payload 含 username | 移除——只留 `{uid, tenant_id, roles}` |
| 8 | `session-` 明文 token | 彻底禁用（不再签发、不再接受） |
| 9 | 三处认证代理（sso.go/whoami.go/http.go:1133） | 删除——chat-token 本地验签 |
| 10 | Gitea 独立账号体系 | 规则 6 SSO 打通（OAuth2 → iam） |
| 11 | RBAC 预留 | 完整落地（roles/permissions/user_tenants + 中间件） |
| 12 | `chat.org_change_audit` 专用表 | 并入通用 `iam.audit_log` |
| 13 | `chat.contacts/user_agents/user_hosts` 旧兼容 | 保留（已是规范三表）——仅类型统一 |

---

## 六、API 规范

| 规范 | 规则 |
|---|---|
| **前缀** | 统一 `/api/v1/`（存量 `/api/` 迁移后废弃） |
| **认证** | `Authorization: Bearer <JWT>`（`{uid, tenant_id, roles}`，本地验签） |
| **响应结构** | `{code, message, data}` 统一；错误码见下表 |
| **分页** | `?page=&page_size=` + 响应 `{total, items}` |
| **错误码** | `0` 成功；`40001` 参数；`40100` 未认证；`40300` 无权限；`40400` 不存在；`42900` 限流；`50000` 服务端 |
| **幂等** | 写操作支持 `Idempotency-Key` header |
| **日志** | 全接口 access log（uid/tenant_id/耗时/状态） |

---

## 七、迁移步骤（停机窗口执行）

1. **备份**：全库 pg_dump（含归档）
2. **DDL**：建 iam schema 全套表；业务/聊天表 `org_id`→`tenant_id`、`user_id` text→BIGINT
3. **数据迁移**：org_user→users（uid 值不变）→accounts（手机/邮箱拆出）→tenants（00000000 平台）→user_tenants；chat 表类型转换
4. **代码重构**：chat-token + marketplace 全部 SQL/ORM/JWT/中间件（uid/tenant_id/RBAC）
5. **依赖更新**：RLS 策略（41 表列名）、审计触发器（并入 audit_log）、漂移检测脚本
6. **清理**：DROP org_user/friends 旧表/兼容视图/host_codes；删代理/旧 token 分支
7. **全量回归**：登录（账号/微信/手机）/聊天/中转/计费/商户隔离（8/12 验证）/设备注册/权限
8. **文档同步**：规则 v2.0 + Marketplace v3.36

---

## 八、验收标准（迁移完成即达标）

- [ ] 全库无 `org_id` 列名、无 `user_id` 列名（uid 全量）
- [ ] 无 `session-` token、无 username 外键
- [ ] `iam.audit_log` 记录所有身份/归属变更
- [ ] RLS 按 tenant_id 隔离验证通过（平台 12/商户 8）
- [ ] RBAC：super_admin/merchant_admin 权限矩阵测试通过
- [ ] 计费单价验证（官方×2、高峰×2）回归通过
- [ ] 旧表/视图/代理全部 DROP，零过渡层

---

## 九、决策结论（按"彻底/零技术债"原则定案）

| # | 决策点 | 结论（定案） |
|---|---|---|
| 1 | 范围 | **全量一次**：命名+表结构+迁移+清理+API 规范+RBAC+Gitea SSO+多租户切换——一次停机窗口 |
| 2 | session- token | 本次同窗口禁用（SSO 停签 + 消费端删分支） |
| 3 | messages sender | `sender_type(user/agent/host) + sender_id TEXT` 多态；agent/host 不进 uid 域；receiver 同理 |
| 4 | 多租户 | `tenant_id` 进 JWT + `POST /api/v1/auth/switch-tenant` 刷新；中间件验证 uid↔tenant 绑定 |
| 5 | phone/email 冗余 | **删冗余**——users 只留 profile，权威唯一在 accounts |
| 6 | platform 命名 | 保留 `public.platform_orders`（前缀即规范，无 schema 重复） |
| 7 | text/varchar org_id 表 | 迁移预检扫描清洗（非法值→平台 or 人工清单） |
| 8 | 停机窗口 | 预估 **2-4 小时**（含回滚预案）；proxy_usage 用加列-分批-换列技术 |
| 9 | Gitea OAuth2 | **纳入本次**（原则零技术债）——OAuth2 client 对接 iam，git 登录自动建号 |
| 10 | owner_uid 回填 | 存量 **NULL**（不编造）；新数据强制填 |
| 11 | API 响应结构 | **纳入本次**（原则零技术债）——`{code,message,data}` + 错误码全量改造（327+ 路由 + 前端适配） |

---

## 十、迁移实施计划（一次停机窗口）

见《12FZ-迁移实施计划-v1.0.md》——阶段 0 预检 → 1 DDL → 2 数据迁移 → 3 代码重构 → 4 依赖更新 → 5 清理 → 6 功能落地（Gitea/API/RBAC）→ 7 回归 → 8 文档同步 + 回滚预案。

---

*版本 v3.1 定案版 · 2026-08-15 · 全量范围，待实施*
